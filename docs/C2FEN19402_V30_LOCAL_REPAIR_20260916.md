# V29 evidence and V30 local corrective candidate experiment

## Prior result and storage

V29 revision `e4381ac814b5ebcb3d8f98d90163ad2f29fe96ab` ran scenario
006 once cold. It failed the Code2 work deadline at 295.43213560000004 s,
with 1,017 of 6,693 contingencies completed on time. Base algorithm time was
52.491232304 s. It has no accepted official objective or exhaustive success
certificate. Its 1,020 terminal native records include late results, which
the controller did not credit. There were 1,301 quality probes, 17 physically
verified probes, two native-cost rejections, and only ten selected quality
reuse results. Probe time was 76.128458574 summed worker-seconds. The full
task sum was 5,554.248154216 worker-seconds; this is not elapsed time.

Archive `docs/evidence/C2FEN19402_TOP5_V29_S006_WORK_DEADLINE_20260916.json`
has zero diagnostics and SHA256
`0ace6e7a63391fbd5f4115ef3b54a08ff227550295f099f7a67379851576839a`.
The retained status SHA256 is
`bf494c92b10e1238ccabcfa293d81d92587fea3994c0f19b56794794d445f7b6`.

After resolved-path, no-reparse, process-liveness, archive and protected-006
checks, only V29's disposable `solution_*.txt` payloads were removed. Its
pre-prune inventory had 1,951 entries; a subsequent recursive check found
zero. Windows free-space observations rose from 50,667,421,696 to
52,490,620,928 bytes. These observations include incidental filesystem
activity and are not an exact file-allocation measurement. Logs, base JSON,
status, compact provenance and partial evaluator records remain.

The earlier V28 cleanup removed 1,310 disposable vector entries. Physical
free space immediately before/after was 51,285,499,904 / 52,557,135,872 bytes,
a net 1.1843032836914062 GiB. Its archive and status hashes are recorded in
`C2FEN19402_TOP5_COLD_20260915.md` and remain intact.

Deleted vectors cannot be recreated from verification records. Raw cases,
Git history, current source, dependencies, unrelated user files and full
protected verified references were not deleted. The protected references
remain old 006/010/069 plus V27 077/095.

## V30 design

The local LP is a bounded candidate generator, not a reduced SCOPF acceptance
model. It uses the current cold run's unchanged primary point, never a prior
run or scenario. Existing exact-parallel and quality-gated within-run routes
remain first. Before the global predictor:

1. Fully rebuild and check the original base for this exact outage. If it is
   feasible and passes the existing native-cost floor, use it directly.
2. Otherwise identify a three-hop neighborhood of the actual outage endpoints
   (or generator bus), using in-service topology with the requested branch
   removed. At most 512 control buses are allowed. A one-hop boundary has
   fixed controls but bounded, penalized imbalance. A too-large neighborhood
   skips this attempt; no source outage is skipped.
3. Make at most two local LP proposals, at most 0.35 s each, with a 0.75 s
   combined cooperative budget including construction and checking. Angle
   and voltage movement is restricted to 0.05 rad / 0.02 p.u. per proposal.
   The original base remains the ramp/commitment anchor. Source PMIN/PMAX,
   Q limits, corrective load bounds, branch constraints and 0.5-p.u. imbalance
   caps are unchanged. The LP's optional, conservative separate P/Q flow
   boxes are off: they are stricter than the actual apparent-flow limits and
   would prevent fixed, already-valid remote branches from remaining fixed.
   All actual linearized branch constraints and full nonlinear checks remain.
4. The LP retains full original linearized rows for its independent audit.
   Controls outside the neighborhood are fixed; outside the boundary, signed
   imbalance is fixed to the reference. Exact fixed-column substitution
   produces the small solver matrix. Constant infeasible rows reject the
   candidate rather than disappearing. The local LP uses fresh single-thread
   HiGHS simplex with presolve and no basis/primal start.
5. Expand the LP result, audit every original row and column, recompute all
   nonlinear AC flows, costs and residuals, then apply the complete original
   AC validator. Accept only finite native objective at or above the existing
   floor `R - 0.01*max(1,abs(R))`. This floor is a work-quality heuristic,
   not a lower bound, global-optimality certificate or MIP gap.
6. Failed/low-quality attempts use the unchanged global predictor/fallbacks.
   No local LP's feasibility alone can certify the contingency. Final success
   still requires exhaustive independent official evaluation of all source
   labels, official infeasibility zero, native residual at most 1e-5 and the
   original 300-second end-to-end boundary.

Compact logs record local attempts, reduced/full dimensions, LP status,
expanded linear residuals, nonlinear residuals, objective, selection and
nested time. Compiler/build, component tests, frozen hashes and run evidence
are recorded below before/after the single experiment, respectively.

## Run registration

All 108 Python component tests and four native CTest groups pass. New tiny
tests compare the all-bus compact LP against its full LP oracle, audit the
expanded rows/columns, preserve fixed boundary generation/load/voltage,
reject invalid masks, fixed excessive imbalance and infeasible constant
thermal rows, verify topology/offline-edge/boundary/work-cap rules, and
exercise the worker's nonlinear-and-cost gate plus its global fallback.
All source bases and input states remain immutable. One initial test used an
inapplicable angle-limit fixture; it was replaced with an applicable thermal
limit witness before the full run. No full case was solved during testing.

Release executable SHA256:
`be3c0bde6dd5aadfb426d7739abe0153a227ae76b5916d73c7c087c591c432f9`.

After component tests pass and the freeze is pushed, run 006 once cold using
`config/c2fen19402_top5_local_repair_v30.json` into
`runs/top5_cold_20260915/v30_s006`. Only after full success should this unchanged
revision/configuration run 010, 069, 077 and 095 once each. A failed gate must
be archived and diagnosed before any new version, not automatically repeated.
All five published fifth-best targets remain open.

## V30 result

Frozen revision `7d94535075e4155d8f3ab57d28012316a80b13c5` ran 006 once
cold. It stopped at the Code2 work deadline after 295.3172803000016 s with
870/6,693 contingencies completed. Base algorithm time was 51.935331719 s.
There is no accepted official objective or final exhaustive certificate.

All 870 native terminal records passed their physical checks, but that does
not replace the missing 5,823 source contingencies. The local route selected
only nine records. Its 1,224 LP attempts reported 939 Optimal, 260 iteration
limit and 25 Infeasible statuses. LP attempts consumed 245.554689748 summed
worker-seconds; the full local route used 344.750750076, inside the total
task sum of 5,469.210161862. Nested counters must not be added together.
Among successful LP attempts, 745 passed the nonlinear check but missed the
work-quality floor, 168 failed the nonlinear check, and nine passed both.

For 430 contingencies with at least one recorded feasible local candidate,
the best local candidate beat the final global native objective in 319 cases,
lost in 102 and tied in nine. Mean local/global objectives on this matched,
biased incomplete subset were 23,243.201296707466 / -489,924.40009177296.
Taking the better of both would give 53,166.881214640176 on this subset, but
that is a diagnostic calculation, NOT a completed solution or official score.
For these 430 records the local route used 246.367431061 worker-seconds and
full tasks used 2,834.440550801. No elapsed speedup is inferred from these sums.

Archive: `docs/evidence/C2FEN19402_TOP5_V30_S006_WORK_DEADLINE_20260916.json`,
SHA256 `395084780faa9c7f1f9d5966214ae1294097d0885090215185fb0a01dc33438e`,
zero diagnostics. Retained status SHA256:
`39103d6980c2b40035c11512f1975ed797e1f43606b25dda48b28d0bb1eb8d97`.
Source hashes and the frozen executable match registration. No other V30
scenario was run.

## V31 registration: retain local feasible incumbents

The quality floor was a search policy, not a source constraint. V30 often
threw away a better, independently verified candidate and then paid for a
new feasibility search. V31 keeps the best verified, finite-objective point
for the exact requested outage across the cold direct candidate and its two
local LP attempts. The original floor still allows an early exit and still
governs cross-outage seed-bank reuse; it no longer discards a valid local
incumbent merely for being below that heuristic threshold.

If the best local incumbent remains below the floor, feed that SAME-RUN
verified state to the existing resident predictor/economic cleanup. The
direct-incumbent guard enters bounded economic cleanup at predictor iteration
zero, rather than searching again for feasibility. If the factorization is
unavailable it retains the verified direct point. Retain the polished state
only after complete nonlinear validation and a strictly better native cost;
otherwise keep the local incumbent. No source bound, cost, ramp, tolerance,
contingency or final acceptance gate changes. Without any feasible local
candidate, use the original fresh/global fallback.

Local LPs now allow up to 10,000 pivots instead of inheriting the global
diagonal-basis path's 1,000-pivot cap. Their construction-inclusive 0.35-s
per-attempt and 0.75-s combined proposal budgets remain, so this permits
productive pivots only while that budget remains. The global repair cap stays
1,000. The subsequent economic cleanup retains its existing three bounded
correction rounds, with no optional per-contingency economic LPs. All time
remains inside the same hard end-to-end deadline.

Logs additionally record incumbent cost before/after polish, nested polish
time and predictor-iteration count. Tiny tests must confirm monotonicity,
full independent validation, original-base immutability, iteration-zero
entry and the unchanged global pivot cap before the run is frozen/pushed.
Run 006 once using `config/c2fen19402_top5_local_incumbent_v31.json` into
`runs/top5_cold_20260915/v31_s006`; only a full pass permits the unchanged
version's four follow-on scenarios. All five top-five targets remain open.

V31 passed all 108 Python tests and all four native CTest groups before the
full run. The worker fixture confirms that a below-floor but fully feasible
local incumbent is preserved, polished at predictor iteration zero, and
never replaced by a worse objective. Its returned state passes the full
independent validator. Local/global pivot caps are checked separately.
Frozen Release SHA256:
`c1869178c39e87d5fbcca2577ffb47dc2e03809d8d6bb50a0887310e893fb79d`.

After matching V30's archive/status hashes, zero archive diagnostics,
no running workers, safe resolved paths with no reparse points, and the
protected full 006 solution/certificate, 1,647 disposable V30 vector entries
were pruned. Their apparent size was 2.824 GiB including links. Physical C:
free space increased from 51,078,893,568 to 52,685,721,600 bytes, recovering
1.4964752197265625 GiB. Zero V30 solution-text entries remain; its status
hash is unchanged. Retained logs and records cannot reconstruct those
deleted vectors. Source inputs, code/history and protected solutions remain.
