# Cold 19,402-bus top-five improvement campaign

## Contract

The objective is to reach at least the fifth-highest feasible competitor raw
objective in GO2 Final Event Division 1 for **all five** C2FEN19402 scenarios.
Targets exclude the ARPA-e benchmark and are recorded without display rounding
in `config/c2fen19402_top5_cached_v1.json`. Published scores are comparison
targets, not global optimality certificates.

Every qualifying run starts from its own unchanged source input, without any
saved solution or other scenario's solution as initialization. Within-run
factorization and verified-state reuse are permitted. Scheduling profiles
contain only task ordering/budgets, not solution vectors. Preserve complete
source contingency coverage, source constraints, the independent `1e-5`
residual gate, official infeasibility zero, and the existing 300-second
normalized-input-to-verification boundary. A partial or timed-out result does
not qualify, regardless of its provisional objective.

The campaign's initial passing baseline is algorithm `a1bebd5`, evidence commit `f926188`.
It remains on `codex/all-network-pass-20260915`. Development is isolated on
`codex/c2fen19402-top5-cold-20260915`. The older economic branch and unrelated
untracked reports are preserved.

## Saved-data diagnosis (no new solve)

| Scenario | Base objective | Mean contingency objective | Total | Fifth-best target |
|---|---:|---:|---:|---:|
| 006 | 185659.214 | -185358.571 | 300.642 | 436687.482 |
| 010 | 241566.788 | -75581.161 | 165985.628 | 534621.593 |
| 069 | 452409.321 | 175226.772 | 627636.092 | 949809.595 |
| 077 | 167983.759 | -81830.424 | 86153.335 | 392060.669 |
| 095 | 199628.768 | -26308.107 | 173320.661 | 434319.523 |

These are the exact saved official evaluation components, rounded only here.
The native approximate base objective can differ from the official objective;
success and comparisons always use the official evaluator. The major current
loss is corrective economic quality, not proof of physical infeasibility.
Base-case economic headroom also remains.

## Experiment v1

Enable the existing bounded, cached-Jacobian economic balance corrections on
feasible predictor outputs, without the old negative-two-million threshold.
Disable both optional per-contingency economic LP stages for this mode. Each
of at most four cached corrections/backtracking rounds can replace its
incumbent only after exact nonlinear validation and a strict objective gain.
No source slack cap, tolerance, commitment, ramp bound, or topology rule changes.
The baseline mode remains unchanged and the new mode is explicitly opt-in.

First cold scenario: 095. Keep its 10-second sparse base cleanup and 40-second
sparse AC economic stage unchanged to isolate the corrective change. Component
tests must pass and the implementation/configuration must be committed before
launch. No repeated timing samples of the same revision/configuration.

The ultimate goal remains all five scenarios at top-five objective quality;
one improving run alone is not completion.

V1 pre-run checks: 63 Python component tests and all four native CTest groups
passed. The native Release binary was rebuilt from this worktree. No full
network solve was used as a development test. This milestone changes only the
opt-in corrective cleanup policy; it leaves the base optimization and default
reliability configuration unchanged.

## Storage and evidence

Initial free C: space was approximately 45.6 GiB. Source inputs, code,
environments, the latest verified baseline, and active runs remain untouched.
Before deleting disposable older solution text, inspect exact contained paths,
verify terminal process state, archive compact evidence and verify its hashes.
Preserved certificates record verification; they do not replace pruned full
solutions for independent re-evaluation. Record every material prune.

## V1 cold result and routing defect

Algorithm `125be97`, scenario 095, stopped at the reserved work deadline after
295.309182600 seconds. It completed 1,716/6,579 contingencies and has no complete
official objective or security certificate. No other scenario was launched.
The source model and all RAW/JSON/CON hashes match the frozen baseline.
The base passed (50.663920457 algorithm seconds); its native objective was
200613.950095, close to the prior native 200614.776785.

The post-stop audit found a routing defect in the new opt-in mode: the existing
economic mode bypasses the first-choice rolling corrective seed, but that
condition tested the old CLI flag rather than the effective solver option.
Consequently the new cached mode still inherited costly controls from unrelated
outages within the same run. Cached correction improved each such incumbent
locally but could still be much worse than rebuilding from the common base.
This was not cross-run initialization, but it defeated the economic policy.

The worker logs contain 1,721 successful terminal records, including five
late-finishing records that the controller did not count as in-deadline.
Among those records: 1,368 cached-polish results averaged 3.6603 solver seconds;
351 rolling-seed direct screens averaged 1.3853 seconds. These are concurrent
per-task wall times, not additive end-to-end times. On the 1,721 matching labels,
native objective change averaged -1501455.63 versus the baseline; this biased
partial-label diagnostic is not a full official scenario score.

V2 corrects the guard to use `fast_options.economic_balance_polish`, covering
both economic modes. Both now start each fresh outage calculation from the
common base produced during that same cold run; rolling seeds remain only a
feasibility fallback after fresh prediction fails. All other V1 settings remain
unchanged except the cached correction cap is reduced from four to one after
the measured V1 cost. The task summaries now retain bounded cleanup attribution
(attempts, backtracking, objective and P/Q slack before/after) instead of losing
those fields when transient vectors are deleted. Future qualification still
requires the complete independently verified official result within the
registered deadline.

V2 pre-run validation passed all 63 Python component tests and all four native
CTest groups after rebuilding the Release binary. The registered JSON stays
unchanged; the implementation revision identifies the routing fix and the
one-correction cap. The published comparison workbook SHA256 is
`56146934c9c90e5eff6477246243caa52d1483df4da24b8a5dc0dc0509ee63f0`.

After verifying the terminal Windows/WSL process state, run containment, absence
of reparse points, and the archived status SHA256, the V1 failed run's 3,395
solution-text entries (including hard links) were permanently pruned. Physical
C: space recovered: 3,057,893,376 bytes. The base JSON, logs, original status,
manifest, results and hash-verified compact archive remain. The deleted partial
solution vectors cannot be independently re-evaluated now. No baseline, source
input, code or environment was deleted.

## V2 cold result and V3 corrections

Algorithm `05fa411` completed 6,578/6,579 contingencies before the work deadline
at 295.175332200 seconds. All 6,579 first screens finished; `CTG_000280` was the
only unresolved corrective fallback. There is no complete official score or
security certificate. The base passed in 50.651912545 algorithm seconds, and
the source hashes are unchanged. The previously saved baseline repaired this
outage after its predictor handed off; that past result is diagnostic evidence,
not initialization for a new run.

Of the completed first-stage records, 6,537 selected cached economic correction
and averaged 0.574111225 solver seconds per task. Their mean native objective
was 145036.163584. This is a selected, incomplete subset using the native
approximate objective, NOT an official scenario score or a top-five claim.
The full goal remains unmet for every scenario in this campaign.

V3 fixes an additional routing defect: `screen_candidate` is explicitly a
direct-only seed-bank probe, but economic mode overrode that request and could
run another optimization. Direct-only now remains direct-only whether economics
is enabled or not. It also resets all low-rank outage updates at the beginning
of each predictor invocation; transformer searches must not inherit a previous
line's update. Exact nonlinear validation is unchanged.

V2's attribution whitelist was too late in the compact-output path: successful
records had already discarded the correction fields. V3 retains the ten
bounded scalar economic fields at their source without serializing state
vectors. Component tests cover the direct-only/base/small/large routing policy
and the scalar-only attribution payload. All 63 Python tests and four native
CTest groups passed after rebuilding V3. The JSON configuration, one-step
correction cap, cold-start contract and 300-second boundary remain unchanged.

Before the next run, the terminal process state, exact contained run directory,
absence of reparse points, and archived status hash were checked. V2's 13,188
disposable solution-text entries were permanently deleted (21.77 GiB logical,
including hard links). Current C: free space is about 47.24 GB. Logs, base and
failed-handoff JSON, official partial evaluation data, manifests and the compact
failure archive are retained; the deleted solution vectors cannot be re-evaluated.

## V3 cold result and V4 work allocation

Algorithm `7868697` again finished 6,578/6,579 contingencies, stopping at
295.125425400 seconds with `CTG_000280` unresolved. No complete official score
or certificate is claimed. The base passed in 51.559633739 algorithm seconds.
Its exhausted predictor handed off with a 0.053176159 residual; bounded Newton
reduced the error but still failed the reactive-balance gate at bus 35330.

The corrected logs retain 6,568 economic attempts, of which 6,533 selected an
improvement. Their mean native objective increased from 935.028909 to
142436.071991 within each attempt. Mean summed P/Q slack fell from 2.426536176
to 0.728432445 p.u. The remaining distribution is highly uneven: its median is
0.019347218, 90th percentile 0.587484012, and 99th percentile 16.518642839 p.u.
These are selected per-contingency diagnostics, not a complete official score.

V4 uses the existing bounded compact LP (including both P and Q) for residuals
categorized as active/reactive balance as well as variable/security bounds.
Previously only the last category could enter that compact stage after Newton,
and the unresolved balance residual went straight to the much larger fallback.
The eligibility ceiling remains 0.35; the acceptance tolerance remains 1e-5,
with complete nonlinear validation after every candidate. Existing LP budgets,
trust regions and source bounds are unchanged.

The cached economic policy allows at most three accepted corrections, stopping
early once summed P/Q slack is at most 0.025 p.u. or no validated objective gain
is found. The 0.025 value is only a work-allocation target, NOT a replacement
physical tolerance or source limit. The measured V3 distribution put only
2,925 of 6,568 attempts above this target after their first correction. Both
optional economic LP stages remain disabled. The experiment is still cold,
fully exhaustive and bounded by 300 seconds.

V4 pre-run checks passed all 63 Python tests and four native CTest groups after
rebuilding. After the same terminal-state, containment and archive-hash checks,
V3's 13,188 disposable solution-text entries were permanently pruned, recovering
11,672,444,928 physical bytes. Its base/handoff states, logs, partial evaluation
data and compact failure archive remain. About 47.09 GB is free on C:.

## V4 fully verified result (goal not yet reached)

Cold algorithm revision `8a6886c21a2554b51bf318b15babadce72186a9d`
completed every one of the 6,579 source contingencies for scenario 095.
The runner returned zero with no failed gates. Independent inspection confirmed
6,580 unique official details including BASECASE, no infeasible labels, official
infeasibility 0.0, base residual 8.881784197001252e-16 and maximum contingency
residual 9.643973418016483e-6. The original model and RAW/JSON/CON hashes match.
The official objective was independently reconstructed from every retained
detail and matched the reported value exactly.

| Scenario 095 result | End-to-end seconds | Official objective | Security / time gate | Top-five gate |
|---|---:|---:|---|---|
| Saved reliable baseline `a1bebd5` | 204.006312800 | 173320.660596904 | PASS | Not met |
| Cold V4 `8a6886c` | 259.334549000 | 341534.900354337 | PASS | Not met |
| Published fifth-place target | -- | 434319.522756719 | Comparator only | -- |

V4 improves the official score by 168214.239757433 relative to that saved
baseline. It remains 92784.622402382 below the fifth-place target. These are
different algorithms, not repeated timing samples. No other scenario has been
rerun with the new policy yet; all five top-five goals remain outstanding.

`CTG_000280` now passed through `bounded_fast_linearized_feasibility_repair` in
35.908668120 solver-call seconds, with residual 8.881784197001252e-16. Its
official score is still poor (-12069206.083489845); resolving feasibility did
not resolve its economic quality. Of 6,569 economic correction attempts, 6,535
selected an improvement. Iteration counts were 3,677 at one, 1,427 at two, and
1,465 at three. Their mean native objective rose from 266.769204 to
154237.233636; use the complete official objective above for comparisons.

### Evidence-directed next improvement

The complete official objective decomposition is:

| Component | Base | Mean contingency |
|---|---:|---:|
| Load benefit | 348005.721005069 | 348002.629373871 |
| Generator cost | 144842.457420404 | 144848.147536334 |
| Bus imbalance penalty | 2207.491624008 | 60283.104221085 |
| Line penalty | 1327.755665554 | 964.493557218 |
| Transformer penalty | 0 | 0 |
| Official objective | 199628.016295103 | 141906.884059233 |

Holding current load benefits and generator costs fixed while hypothetically
removing all bus/thermal penalties gives 406317.745422202, still below the
target. This is an accounting counterfactual, not a feasible solution or a
global bound. It establishes that penalty cleanup alone cannot finish the
goal at the current dispatch. The next work must improve genuine economic
dispatch/load decisions as well as the remaining corrective penalties.

Candidate directions to inspect before another experiment are the primary
fixed-commitment PWL optimization's convergence and an economically optimized
common corrective reference constructed inside the same cold run, with every
actual outage still checked against the ORIGINAL base-dependent source bounds.
No prior-run solution may initialize either stage. A common reference would be
candidate generation, not a replacement for exhaustive contingency checking.
These directions are not implemented or claimed successful in V4.

### Retention and cleanup

The new V4 full solutions are retained. Its compact evidence archive SHA256 is
`382791cc4a0971b267da5aa3fdd6299579f01d135d1a68e0529b2a00b03cc1e1`.
The older 095 baseline was separately archived and its status hash verified;
after terminal-process and exact-path checks, only its 13,206 disposable
solution-text entries were permanently removed. This recovered 11,670,409,216
physical bytes and left about 46.92 GB free. The old score records, source
inputs, base JSON, logs and official detail/certificate records remain, but its
deleted solution vectors cannot be independently re-evaluated. Other scenarios'
retained solutions, environments and code were not pruned.

## V5 registered experiment: checked PWL epigraphs

The primary sparse fixed-commitment AC optimizer previously evaluated PWL
generator costs and negative load benefits directly, averaging adjacent slopes
at a breakpoint. V5 adds an opt-in exact epigraph representation. For a signed
source curve with nondecreasing slopes, an auxiliary cost variable is bounded
below by each affine segment, and its positive weighted value enters the
minimized objective. Scaling is algebraic only. Any decreasing slope, nonfinite
coefficient or degenerate segment retains the original representation; no
source curve is convexified or slope-repaired.

Physical variables, bounds (including exact PMIN and source ramp bounds),
physical rows, commitment, and source initialization remain unchanged. Cost
auxiliaries are excluded when converting the NLP vector back to physical
state. Every candidate is rebuilt, rescored using the original source-derived
function, and independently checked before selection. This change does not
make the nonconvex AC problem globally optimal and does not establish a
top-five result without a complete official evaluation.

Configuration `config/c2fen19402_top5_epigraph_v5.json` retains V4's corrective
policy and the 10-second linear / 40-second nonlinear primary allocation.
It registers all five scenarios, the original cold-start contract, the original
source inputs, exhaustive contingencies, and the 300-second end-to-end limit.
The first experiment is one cold scenario-095 run; wider coverage follows only
after assessing that result. The full five-scenario objective remains open.

Pre-run checks: 64 Python tests passed; all four native CTest groups passed
after the final rebuild. Tiny tests compare original and new physical bounds,
starts and rows, map source costs exactly at breakpoints and segment interiors,
reject nonconvex/degenerate source curves, check analytic cost derivatives by
finite differences, and independently validate an optimized tiny state.
The retained V4 full solutions will not initialize this experiment.

## V5 cold result and V6 incumbent correction

Revision `749c470693ea3057565e9f9aa72fb5d148586e20` stopped at
295.328382000 seconds with 4,406/6,579 accepted completed contingencies.
There is no complete official score, exhaustive certificate, or top-five
claim. Eight additional terminal worker messages arrived outside the accepted
completion count; they do not turn this timeout into a successful run.

The primary nonlinear stage used 41.310779170 seconds and 30 iterations. All
13,188 active curves passed the checked epigraph conversion (54,883 rows, no
fallback curves). Its selected physical state passed with residual
1.7763568394002505e-15. The official BASECASE objective alone improved from
199628.016295103 to 204992.986761500. That is not a complete scenario score.

Among the 4,414 labels with terminal worker records in both versions, mean
solve-call time rose from 0.784102047 seconds in V4 to 1.183614873 in V5.
This matched-label diagnostic is not an end-to-end benchmark comparison or an
unbiased estimate for the missing labels. Mean predictor iterations were
2.374943362 and 2.649750793 respectively. The incomplete V5 economic-attempt
subset also needed more backtracking; the improved primary dispatch did not
make corrective processing easier automatically.

Inspection found a separate incumbent-selection defect: economic mode skipped
an already feasible direct-outage state to enter the predictor, but did not
preserve that state's objective against a worse feasibility-directed result.
V6 fully rebuilds and validates that direct candidate under the ORIGINAL
base-dependent contingency context. When it beats the predictor (or the
predictor is infeasible), economic cleanup starts from the verified incumbent
and may accept only verified improvements. If the predictor is unavailable,
the verified direct state remains a feasible fallback. Invalid direct states
still require the unchanged corrective feasibility process.

Three scalar fields record direct feasibility, its native objective and
incumbent selection. Tiny two-bus generator-outage tests exercise this exact
production path by lowering only the size-routing threshold in the test;
production retains the 16,000-bus threshold. Direct-only seed probes remain
validators rather than optimization runs. No prior-run solution is used.

The V5 timeout archive SHA256 is
`1a2dd445c90ccf5968e8f4e5707dfd6f6883719d1ba8e9621c4c725c9711b81b`.
V4 remains the retained, fully verified incumbent for this study.

V6 pre-run tests passed all 64 Python tests and four native CTest groups.
The two-bus test explicitly enters the incumbent replacement branch, checks
nondegradation with economic polish both disabled and enabled, rejects an
invalid direct probe, and preserves the feasible incumbent when predictor
routing is disabled. These objective comparisons use the native objective;
the pinned official evaluator remains the benchmark scoring authority.

V5's terminal state, archive/status hash and exact contained paths were checked
before removing 8,819 disposable solution-text entries (including hard links).
Physical recovery was 7,850,983,424 bytes. The source inputs, code, environment,
logs, base JSON, partial evaluator details and compact timeout archive remain.
Deleted V5 solution vectors cannot be independently re-evaluated. No active
run or retained V4 full solution was pruned.

## V6 cold result: incumbent invariant holds, deadline still fails

Algorithm `c9efbc20d04196a95c6a7d5a621be5435734280d` stopped at
295.260389200 seconds with 5,765/6,579 accepted completed contingencies.
All source hashes match V4/V5. The run is incomplete: no final official score,
complete security certificate or top-five placement can be assigned.

There were 5,779 terminal worker records, including late arrivals outside the
accepted completion count. Of those records, 2,861 contained a fully verified
direct incumbent; the new comparison selected it instead of the predictor 37
times. None of those 2,861 final native objectives was below its verified
direct incumbent (comparison allowance 1e-6). This is evidence that the new
incumbent invariant held, not an exhaustive scenario feasibility certificate.

The 5,765 recorded economic-polish attempts used one/two/three iterations on
2,969 / 1,072 / 1,724 tasks. Their mean backtracking count was 2.065221162.
On the 4,414 labels shared with V5's terminal records, mean solve-call time
was 1.080403306 seconds, versus V5's 1.183614873. On 5,779 labels shared with
V4, V6 still averaged 0.923562719 seconds versus V4's 0.675097089.
The time-budgeted cold primary solves selected slightly different states, so
these comparisons do not isolate a pure causal speedup from the incumbent fix.

V6's primary algorithm used 51.909953576 seconds. The sparse AC stage used
41.527113293 seconds and 29 iterations, selecting a verified intermediate
with native objective 206790.632721708 and physical residual
8.881784197001252e-16. The official BASECASE-only objective was
204777.445882805. Its load benefit / generator cost / bus penalty / line
penalty were 341698.896402296 / 129089.477847715 / 5469.015910426 /
2362.956761351. These base-only figures must not be substituted for the
missing complete scenario score.

| Scenario 095 experiment | End-to-end seconds | Accepted contingencies | Complete official objective | Outcome |
|---|---:|---:|---:|---|
| V4 `8a6886c` | 259.334549000 | 6579/6579 | 341534.900354337 | Verified; below top five |
| V5 `749c470` | 295.328382000 | 4406/6579 | -- | Timed out |
| V6 `c9efbc2` | 295.260389200 | 5765/6579 | -- | Timed out |

V4's retained summary was rechecked: complete 6,580-detail label set,
official infeasibility 0.0 and maximum independent contingency residual
9.643973418016483e-6. It remains the best fully verified study result.
The goal still covers **006, 010, 069, 077 and 095**. None of their top-five
requirements is satisfied by these experiments. No other scenario was run
with V5 or V6; failed settings are not being promoted as a new default.

### Next design work, not yet implemented

The incumbent fix is worth retaining but was exercised too infrequently to
remove the main economic/runtime deficit. A stronger next candidate is a
shared economic corrective reference constructed INSIDE each cold run, using
the original base-dependent corrective bounds and time intervals. The base
solution must remain the authoritative context; a reference may never shift
the final ramp bounds, drop an outage, or stand in for exhaustive validation.
It should amortize common corrective economic work instead of doing the same
work thousands of times. Before any full experiment, tiny tests must prove the
base/recourse bound distinction, exact source costs, and independent checking.
All reference construction and processing must fit the same 300-second total
budget. Improving the primary NLP's convergence is another unresolved issue.

### V6 evidence retention

The terminal timeout archive SHA256 is
`ff3f5d141dbfbddcfba3650a3cb875bfc1d9462e62f648234c2b33a33a93414d`.
After checking the archived status hash, terminal processes, containment and
absence of reparse points, 11,545 disposable V6 solution-text entries were
permanently removed, recovering 10,280,722,432 physical bytes. About 46.44 GB
was free afterward. Logs, base JSON, partial official details, source inputs,
code and the compact evidence archive remain; those deleted V6 solution
vectors cannot be re-evaluated. V4's full verified solutions remain intact.

## V7 registered experiment: shared within-run corrective reference

V7 implements the next design, without loading any prior-run solution. After
this cold run has independently verified its own base, a separate 30-second
sparse AC optimization constructs a common corrective candidate on the intact
topology. Its generator/load ramp bounds use the ORIGINAL base dispatch and
source corrective ramps; costs use `delta_ctg`, thermal constraints use
`RATE_C`, and the angle-limit applicability test uses the original base angles.
Exact PMIN/PMAX, source cost curves and all other source limits are retained.
The same checked PWL epigraph representation is used for eligible curves.

This intact-topology candidate is not a source contingency, does not count
toward the label set, and is not itself a security certificate. Its construction
time is included in the same 300-second end-to-end budget and 110-second base
process allowance. The 10-second linear and 40-second nonlinear base allocations
remain unchanged. Only a verified native-objective improvement is made available
as the shared candidate; failure preserves the original base and normal workflow.

The base state is serialized before/after reference construction and checked
for mutation. Each resident worker revalidates the reference against that base.
The reference may supply the predictor's start and Jacobian linearization point,
but NEVER its constraint anchor. A predictor detail was corrected accordingly:
load-ramp intervals must use original-base load, not supplied-candidate load.
Every actual outage is still rebuilt and checked separately. A better verified
direct application of the original base is retained as another incumbent.

Pre-run tests passed all 65 Python tests and four native CTest groups after the
final rebuild. Tiny fixtures check exact positive PMIN, offline-generator bounds,
distinct source/base/candidate dispatch, corrective load/generator ramps,
corrective-period objective consistency, emergency thermal rows, analytic
constraint/objective derivatives, rejection of an out-of-ramp candidate, base
immutability, and a real two-bus outage using the separate reference. CLI tests
check opt-in routing, the unchanged deadline/evaluator, and rejection of invalid
reference budgets. No full production scenario was used as a development test.

The first V7 experiment is one cold scenario-095 run using
`config/c2fen19402_top5_shared_reference_v7.json`. All five target scenarios stay
in scope. A successful complete run is required before comparing a scenario
score or promoting the settings; component tests alone cannot establish either.

## V7 cold result: shared reference did not pay for itself

Cold revision `80b017c2723b352c6a25d0e44151d05991be7e84` stopped at
295.321778600 seconds with 2,325/6,579 accepted completed contingencies.
There is no complete official score or security certificate. All source hashes
match the previous experiments. The reference was generated inside this run,
the base was unchanged, and it was never counted as a contingency.

The primary algorithm including reference construction took 80.555769980
seconds. The reference stage used 30.126443169 total seconds (20 Ipopt
iterations), selecting its ninth intermediate iterate. That state's native
corrective objective was 208135.213827088, versus 206802.439523403 for the
original base evaluated with corrective semantics; its independently rebuilt
residual was 8.881784197001252e-16. The final iterate was also physically
verifiable after rebuilding its allowed slacks, but had a much worse native
objective, -248203.504850245, and was correctly not selected. Neither stage
reached an optimal Ipopt termination before its wall-time budget.

All 2,332 terminal worker records (including seven late arrivals) reported use
of the common reference. Mean/median solve-call time was 1.954497456 /
0.536518477 seconds. There were 2,271 selected economic-polish results, 23
predictor results, 29 bounded Newton rescues, two bounded linearized repairs,
and seven direct seed-bank screens. The original-base direct candidate was
verified on 186 records; none was worse than that candidate on final native
objective, and none required selecting it over the reference result.

The settings are NOT promoted. The next work is improving sparse AC NLP
convergence itself, specifically a tested exact sparse Lagrangian Hessian for
the smooth checked-epigraph model in place of its current limited-memory
approximation. This does not change the physical model or source costs. The
shared-reference stage should remain off in that next isolated experiment,
because V7 supplied too little benefit for its cost. Exact derivatives must
pass tiny-fixture finite-difference checks before any new cold full run.

The V7 timeout archive SHA256 is
`b4559f10f4ea968ad2c1983c83bf6bdd8ed9d34943f0464285d0dfcdb10eff99`.
All five top-five gates remain unmet; V4 remains the retained fully verified
study result, with its complete solution payloads intact.

### V7 evidence retention

After verifying the compact archive/status hash, terminal process state, exact
path containment and absence of reparse points, 4,613 disposable V7 solution
text entries were removed. Physical recovery was 4,147,339,264 bytes. Base
JSON, logs, partial official details, source files and code remain. Deleted
V7 vectors cannot be independently re-evaluated; V4's full vectors remain.

## V8 registered experiment: exact sparse Lagrangian curvature

The sparse fixed-commitment AC NLP can now opt into an exact analytic Hessian
of the Lagrangian. Its lower-triangle pattern is constructed once, shared
coordinates from parallel branches are summed, and repeated coordinates from
self-loops are handled with the full symmetric contribution. Curvature covers
nodal balance, fixed shunts, both branch thermal sides and thermal slack;
generator/load bounds, reference/angle rows and the checked PWL epigraph are
linear. The objective is linear after that cost transformation, so its Hessian
is zero. Exact mode refuses any curve that failed the smooth epigraph check.

The first finite-difference test exposed a pre-existing sign error in this
sparse NLP's from-terminal reactive-flow coefficient for phase shifters:
`(b*tr - g*ti)/tap^2` must be `(b*tr + g*ti)/tap^2`. The latter matches complex
power `V * conj(YV)`, the nodal admittance model and the unchanged independent
checker. This is an implementation correction to source physics, not a new
physical formulation. Previously accepted solutions were independently
rebuilt/checked and are not invalidated by an incorrect internal optimizer row.
The experiment therefore measures the combined correction and exact Hessian;
it must not be described as a pure isolated Hessian speedup.

All four native CTest groups and 66 Python tests passed. Tiny fixtures check
the complete Lagrangian Hessian against finite differences of `grad(f)+J'lambda`
for parallel branches, taps/phase shifts/end shunts, transformers, self-loops,
inactive branches, base and corrective ratings, and zero constraint multipliers.
They independently compare terminal power to complex-admittance arithmetic,
check the unchanged bounds/constraint values between derivative modes, reject
unchecked nonsmooth curves, and run a tiny exact-Hessian solve whose logged
Hessian calls and independently verified nondegrading output are required.

The V8 configuration leaves the failed shared-reference stage OFF. Primary
budgets remain 10 seconds linear plus 40 seconds AC, with a 300-second total,
the same source data/constraints and original cold start. All five scenarios
remain registered; the first full experiment is scenario 095. No full case was
used as a component test. Exact derivatives do not certify global optimality;
complete independent feasibility and official scoring remain mandatory.

## V8 cold result: better primary economics, incomplete outage processing

Revision `fd3eec54cfbb80c8e3356674496cdc01fb3c3dad` stopped cleanly at
295.325449900 seconds with 3,501/6,579 accepted completed contingencies.
Source hashes match the earlier cold experiments. There is no complete
official scenario score or security certificate and no top-five claim.

The base algorithm took 50.772304452 seconds. Its AC stage used 40.418625221
seconds, 57 iterations and 57 exact-Hessian evaluations with 249,289 unique
lower-triangle entries. All 13,188 source curves passed the epigraph check.
It selected verified intermediate 56 with native objective 216129.241419777;
the final point was worse and was not selected. The BASECASE-only official
objective was 214720.193886003, not the complete scenario objective. The solver
still returned Maximum_WallTime_Exceeded, not optimal termination.

There were 3,511 terminal worker records including ten late records: 3,482
economic-polish results, 25 predictor results and four bounded Newton rescues.
Mean/median solve-call time was 1.560528316 / 0.569118693 seconds. Mean economic
backtracking attempts rose to 4.704357733, versus 1.242134063 over V4's complete
record set. V8 used three polish iterations on 1,879 terminal records. These
are different accepted/terminal label populations and time-budgeted primary
solutions, so this is bottleneck evidence, not a controlled causal speedup.

V8's rebuilt base P/Q slack sums were 0.005519618 / 0.004264462 p.u.; its
largest branch overload slack was 0.199214202, close to its allowed bound.
Reduced aggregate base imbalance therefore does not by itself imply easier
corrective searches. The rejected polish trials currently pay for full PWL
economic reconstruction and full validation. The next implementation candidate
is staged trial checking: rebuild/check physical fields first, then reconstruct
economics and complete the omitted checks only for physically eligible trials.
Every accepted candidate must still receive all checks, with unchanged steps,
objective comparison, bounds and tolerances. Tiny equivalence tests are required.

The V8 timeout archive SHA256 is
`c4b9ad125698d636acb81443e0a0b073cc1d4e59f23b70ce144c4fa6be0733f5`.
V4 remains the fully verified study incumbent. All five top-five gates remain
unmet; the other four scenarios have not been run with V8.

### V8 evidence retention

The archived status hash was independently matched before pruning; no native
solver or runner was live, and the exact contained target had no reparse
points. Removing 7,010 disposable solution-text entries recovered
6,275,723,264 physical bytes, leaving about 45.94 GB free. Logs, base JSON,
partial official details, source and code remain. Deleted V8 vectors cannot
be re-evaluated. V4's full verified vectors are still retained.

## V9 registered experiment: staged economic-polish trial checks

Corrective economic backtracking now rebuilds physical quantities and checks
the complete physical constraint set before rebuilding PWL weights/costs.
Physically rejected trials stop there. A physically eligible trial receives
the economic reconstruction and every previously omitted PWL/Ohm-law check
before it can be selected. The interpolation steps, objective comparison,
source limits, immutable base context and validation tolerance are unchanged.
Final independent checking and official exhaustive evaluation are unchanged.

Tiny equivalence tests compare the new path against the original full-rebuild
and full-validator path for branch and generator outages, positive PMIN,
violated generator/load/voltage/thermal bounds and a near-tolerance voltage.
They require identical accept/reject decisions, exact objective arithmetic and
rebuilt states for fully checked candidates, and equality of all twelve
numerical residual categories. They also require both early rejection and
full acceptance paths and verify that the base is not mutated. All four native
CTest groups and 66 Python tests passed after the final build.

New counters/time attribution distinguish total trial checks, physical
rejections, economic completions, physical-check time and economic-check time.
This will establish whether invalid-trial economic work was material rather
than assuming that every backtrack was caused by a physical violation.

V9 retains the V8 exact primary Hessian and phase-shifter correction. The
shared-reference stage remains off, budgets remain 10+40 seconds for primary
refinement and 300 seconds total, and all five cold scenarios remain in the
registered configuration. First full experiment: one cold scenario-095 run.

## V9 cold result: no demonstrated end-to-end improvement

Revision `3e39aa2cb3a7f0c98d751585cdb2baa094c2a2af` stopped at
295.292905300 seconds with 3,522/6,579 accepted completed contingencies.
All source hashes match. There is no complete official scenario objective,
exhaustive certificate or top-five claim. The completion count is essentially
unchanged from V8; this experiment does not establish a speed improvement.

The base algorithm took 51.298637797 seconds. The AC stage used 40.940919226
seconds and 58 iterations/evaluations, again selecting intermediate 56 with
native objective 216129.241183692 and independent residual
1.7763568394002505e-15. This objective differs from V8 by less than 0.001,
but cold wall-time-limited runs are not bit-identical controlled repetitions.

There were 3,533 terminal records including 11 late records: 3,504 economic
polish selections, 25 predictors and four bounded Newton rescues. Aggregate
solve-call time was 5465.847062075 seconds across workers; mean/median call
time was 1.547083799 / 0.545907553. This aggregate is not end-to-end wall time.
There were 16,547 backtracking attempts, 1,154 verified direct candidates and
86 selections of a better direct incumbent.

The new detailed counters were missing from the compact worker payload:
they had been added to the full serializer and final whitelist but not the
intermediate economic-summary serializer. Missing measurements are UNKNOWN,
not zero. They cannot establish how many trials avoided economic work. This
logging defect does not change the solver path or the observed timeout.
The serializer is now corrected, with a tiny regression requiring all five
nonzero counters/times to survive both full and compact serialization. No
replacement full run has been performed solely for this logging correction.

Archive SHA256:
`a13640525017993a7d6f029d728ed5b96fc40e60f82bc10f0141dac3fccb32d9`.
All five top-five requirements remain unmet. The full retained V4 result is
still the study incumbent; V8/V9 are not promoted as successful replacements.

### Final V9 diagnostics and retention

The compact-serializer fix passed all four native CTest groups and 66 Python
tests after rebuilding. No full benchmark was launched after that fix. The
missing V9 detailed counters are not reconstructed or represented as measured.
Further attribution needs to separate outage-update preparation, cached
corrections and staged trial work before selecting another runtime change.

The terminal V9 archive/status SHA256 was verified before pruning, along with
absence of active runners/solvers and reparse points under the exact target.
Removing 7,031 disposable solution-text entries recovered 6,313,226,240
physical bytes, leaving 45,830,742,016 bytes free. Logs, base JSON, partial
official details, code and source data remain. Deleted V9 vectors cannot be
re-evaluated; V4's full verified solution payloads remain untouched.

## V10 registered experiment: bounded inverse-selector reuse

The fixed-Jacobian outage update forms `J^-1 E` for the affected equation-row
selectors. This intermediate depends only on the current immutable numerical
factorization and selector rows, not the candidate dispatch or branch derivative.
Repeated requests can therefore reuse exactly those columns while recomputing
the branch derivative and small Woodbury system for the current outage/state.

Each resident worker now retains at most eight selector matrices per full,
active and reactive factorization, with LRU eviction. A different factorization
owner/dimension clears its cache; contingency-specific refactorizations create
new cache objects. The production factorizations are not numerically modified
in place. This is within-run algebraic reuse, not solution seeding: no previous
run's commitment, voltages or dispatch is loaded. Resetting the active outage
update still occurs before every new contingency; inverse selectors cannot
carry a previous branch's physical update into another outage.

Tiny tests verify cache hits, disabled-cache equality, equation residuals,
Woodbury solves with distinct branch derivatives using the same selector,
bounded LRU eviction, owner invalidation, invalid-index rejection, and exact
equality of complete tiny outage states with caching enabled/disabled. They
also prove a reused cache has hits without changing the resulting state.
All four native CTest groups and 66 Python tests passed after the final build.

Timing/counters now cover fixed-Jacobian preparation, outage-update management,
inverse-selector solves/cache hits/misses/bytes, total economic polish and its
cached corrections, plus the V9 physical/economic trial checks. These are
nested timings: do not sum parent polish time with all its child times.
Full and compact serializers share the new timing payload, and tests require
all nonzero fields to survive both serialization routes. The native worker
whitelist explicitly includes them; their survival there is checked from the
full experiment's emitted records rather than inferred from the unit tests.

V10 otherwise retains V9's cold primary path, exact Hessian, source constraints,
all-source-contingency checks and 300-second total. The shared reference remains
off. All five scenarios remain registered; first full experiment is one cold
095 run. The cache's performance benefit is unproven until that experiment.

## V10 cold result and measured bottleneck

Revision `6918463b476302b49598b1393c8821a92d262c4d` stopped at 295.323191900
seconds with 3,560/6,579 accepted completed contingencies. All source hashes
match prior runs. There is no complete official score or exhaustive certificate.
The 50.791105090-second base stage selected the same iteration 56 as V8/V9,
with native objective 216129.241342939 and residual 1.7763568394002505e-15.

All 15 new/previous timing fields survived in all 3,571 terminal records,
including 11 late records. Summed worker solve-call time was 5487.806661371
seconds (not end-to-end wall time), with mean/median 1.536770278 / 0.536838737.
The cache recorded 4,797 hits and 8,520 misses, with a sum of per-worker peak
retained matrix payloads of 347,931,648 bytes. WSL reported zero swap usage.
The completion count remains close to V8/V9, so no material speedup is claimed.

| Observed worker phase | Summed seconds | Interpretation |
|---|---:|---|
| Fixed-Jacobian preparation | 468.882624 | Initial and contingency-specific preparation |
| Outage-update management | 144.823835 | Includes the inverse-selector solves below |
| Inverse-selector solves | 125.850881 | Nested inside outage-update management |
| Economic polish | 1048.997152 | Includes corrections, checks and some update work |
| Economic cached corrections | 140.707303 | Nested inside polish |
| Physical trial checks/rebuilds | 479.575172 | Nested inside polish |
| Economic reconstruction/complement checks | 75.108912 | Nested inside polish |

There were 24,618 trial checks: 15,003 physical rejections and 9,615 complete
economic checks. Thus staged rejection did avoid economic work, but that work
is not the dominant bottleneck. Parent and child timings must not be summed.

Among terminal records, 334 tasks needing more than ten predictor iterations
used 2607.091334712 summed solve seconds: about 9.4% of tasks and 47.5% of their
summed solve-call time. The worst examples used 37-49 iterations and roughly
32-41 seconds each, with only about 0.3-0.8 seconds of economic polish each.
Another 667 tasks needing 4-10 iterations used 1331.241002283 seconds.
These are observed terminal-task aggregates, not uncensored full-run CPU shares.

The next change should target slow feasibility corrections, not primarily
economic serialization or inverse-selector work: bounded adaptive local
Jacobian refresh, retaining any already-better candidate and the original
complete validation. Existing code can keep a stale contingency Jacobian while
accepting many tiny improvements. A fresh derivative must be tested without
changing source constraints, tolerance, or the original corrective base anchor.

Archive SHA256:
`4ace175e50e446a09c4b1e7a3019109275099b4dd062a530a11d13d89d19fa3b`.
All five top-five gates remain unmet; V4 is still the retained verified study
incumbent. No other scenario has been run with V10.

### V10 retention

The terminal archive and current run-status SHA256 were matched before pruning,
with no active runner/native solver and no reparse points under the exact run
directory. Pruning 7,070 solution-text entries recovered 6,381,092,864 physical
bytes, leaving 45,616,611,328 bytes free. Logs, base JSON, partial official
details, code and source data remain; the deleted V10 solution vectors cannot
be independently re-evaluated. V4's complete verified payload remains intact.

## V11 registered experiment: bounded adaptive Jacobian refresh

V10's observed long-tail feasibility searches motivate a local derivative
refresh rather than another optimization of the small outage-update phase.
The cached-economic study mode now permits at most two adaptive refreshes per
branch contingency. After four predictor iterations, a refresh is eligible if
the residual remains above ten times the unchanged validation tolerance and
has fallen by less than 30% over the last four iterations. A four-iteration
cooldown applies after any contingency-specific Jacobian preparation.

The fresh factorization uses the current within-run candidate and the actual
outaged branch. It probes the existing five dampings of a coupled Newton
direction. An already-selected correction is retained unless a fresh trial
has a strictly smaller fully checked physical residual. The fresh cache is
retained only when one of its probes improves that incumbent. Source bounds,
PMIN, ramp intervals, cost curves, base anchor and all final acceptance checks
are unchanged. No previous run's solution is loaded. Ordinary options remain
adaptive-off; this experiment explicitly uses cached-economic mode.

Tiny tests cover the trigger/cooldown/cap, disabled and generator-outage paths,
good progress, near-tolerance and nonfinite residuals. A two-bus parallel-line
outage forces one refresh probe, verifies the complete result independently,
checks that no better correction or immutable base was lost, and checks the
disabled mode. Both serializers and the worker whitelist include refresh
counts, selected counts, elapsed time and before/after residuals. All four
native CTest groups and 66 Python tests passed before registration.

The exact primary Hessian, 10+40-second primary-refinement budgets, disabled
shared-reference stage and 300-second end-to-end deadline remain unchanged.
All five scenarios remain registered. First full experiment: one cold 095 run.
Neither a runtime improvement nor a top-five result is assumed from unit tests.

## V11 interrupted by checkpoint replacement failure

The single cold run at revision `765bd49bcd1174dcd0fff258900abc8c1f23d97f`
ended after 83.641036800 seconds, with only 70/6,579 accepted completed
contingencies. This was not the 300-second deadline and not an observed solver
infeasibility: the runner failed inside `write_json` while atomically replacing
`run_status.json`, with Windows error 5 (access denied). The base stage took
51.196505806 seconds and passed independent validation at
8.881784197001252e-16. All source hashes match. There is no full official score,
exhaustive certificate, runtime-comparison conclusion or top-five claim.

The monitor had streamed the large live checkpoint through line-oriented
PowerShell output. A tiny Windows test reproduces the same replacement failure
when an ordinary reader holds a checkpoint open, and replacement succeeds with
the same account/path after that reader closes. The live file is not read-only;
the account has full control. This establishes a plausible sharing-collision
failure mode, not proof of the exact live handle owner (no handle trace was
captured). No ACL, permission, solver or checkpoint-writer retry policy changed.

`scripts/read_run_progress.py` now copies checkpoint bytes, closes the handle,
then decodes and emits only scalar progress fields. It does not stream the full
contingency schedule. Tests verify replacement can occur before decoding, the
old snapshot remains coherent, missing data is not silently invented, missing
files are not hidden/retried, and OneDrive paths are rejected. This reduces
reader hold time; it is not a guarantee against every external file locker.
The monitor explicitly labels its snapshot as not proof of process liveness.
Use it together with the live runner handle, not as a replacement for that handle.

The Windows Python suite passed 72 tests. All six new monitor tests passed on
Windows; five passed on WSL and the Windows-specific negative control was
skipped there. The algorithm's four native CTest groups had already passed
before its frozen run. The native algorithm and benchmark configuration remain
unchanged after that run. A replacement full run awaits user confirmation.

Archive SHA256:
`6c44bc0b74fcd85ac85e41bd8b3056e2868eec860fe5a5924436616e5d075e2b`.
The archived/current run-status hashes match. V11 payloads are retained, no
solver is left running, and V4 remains the complete verified study incumbent.
All five top-five requirements remain open.

### V11 follow-up audit while replacement approval is pending

The original two-bus regression required a probe and a feasible outcome but
did not require the refreshed direction to win. The strengthened test now
requires one actual selected refresh, independently validates a second,
different branch outage using the same worker, and checks that the second
outage has zero flow on its own outaged branch while the first branch is
active again. Both solves preserve the immutable original base. Additional
checks require zero refresh attempts when the work cap is zero and reject
negative window/cap settings.

All four native CTest groups passed after rebuilding. These changes are
confined to the component-test routine; no production solver condition,
configuration, tolerance or source input changed. No full replacement run
has been started. A future run must record its actual new Git revision and
binary hash rather than claim binary identity with the earlier V11 attempt.

### V11 single replacement registration

The continuing goal instruction authorizes further cold iteration. Register
one replacement scenario-095 run using the unchanged V11 configuration and
production algorithm after the monitoring fix and stronger tiny tests. This
requires no account/ACL change. The new output directory is
`runs/top5_cold_20260915/v11_replacement_s095`; the interrupted attempt remains
separate. Monitor through the live process handle and the compact close-before-
decode reader, never by streaming the complete live checkpoint. The runner
records the actual frozen commit, executable hash, source hashes and config.
All original acceptance gates and the 300-second deadline remain binding.

## V11 replacement result: helpful on some cases, still incomplete

Frozen revision `996479c5a1dac35468aab0b417b038a6c4c51780` stopped at
295.291463300 seconds with 3,767/6,579 accepted completed contingencies.
The final error was `fast screen CTG_002242 finished after the end-to-end work
deadline`. There was no recurrence of the checkpoint access error. All source
hashes match. No complete official objective or exhaustive certificate exists.

The base stage took 50.816281732 seconds, selecting verified intermediate 56
with native objective 216129.241511713 and independent maximum residual
1.7763568394002505e-15. This is a base-only native objective, not a full official
score. The AC refinement stopped on its unchanged 40-second work budget after
59 iterations; its earlier independently verified incumbent was retained.

The completed count exceeds V10's 3,560 by 207, but both are censored single
cold runs that failed the end-to-end gate. This does not establish a general
speedup or a successful replacement for V4.

All five adaptive-refresh fields survived in all 3,773 terminal records,
including six late records. Summed worker solve-call time was 5484.494689551
seconds and median call time was 0.502942804 seconds. These are parallel-task
aggregates, not end-to-end wall time. There were 236 refresh probes and 87
selected refreshes across 181 attempted contingencies; 82 contingencies selected
at least one refresh and 99 selected none. No recorded refresh lost its existing
better residual. Refresh work consumed 207.241126225 summed seconds, including
factor preparation; total factor preparation was 591.121141161 seconds and
economic polish was 1062.952402390 seconds. Nested times must not be added.

| Terminal-task group | Tasks | Summed solve seconds | Summed refresh seconds |
|---|---:|---:|---:|
| No adaptive refresh | 3,592 | 3732.533600 | 0 |
| Refresh attempted, none selected | 99 | 1285.446366 | 130.105771 |
| At least one refresh selected | 82 | 466.514724 | 77.135356 |

Matched labels demonstrate that the change can help particular searches:

| Contingency | V10 predictor iterations | V11 predictor iterations | V10 solve seconds | V11 solve seconds |
|---|---:|---:|---:|---:|
| CTG_002247 | 170 | 8 | 12.665707 | 3.168760 |
| CTG_000546 | 73 | 6 | 5.434081 | 1.790031 |
| CTG_003937 | 13 | 5 | 7.001672 | 1.752199 |
| CTG_006288 | 49 | 49 | 41.391 | 42.285175 |

All 3,571 V10 terminal labels appear in V11. Their summed solve time changed
from 5487.806661371 to 5384.797352121 seconds and predictor iterations from
11,220 to 10,806. This is a descriptive matched-label observation: base
iterates are extremely close but not bit-identical, parallel scheduling differs,
and this is not a repeated controlled timing sample.

The worst pair CTG_006288/CTG_006287 still used 49 iterations and rejected both
fresh-Jacobian probes. CTG_004464 used 48 iterations and likewise rejected
both probes. Therefore fresh derivatives alone do not fix the long tail.
The next candidate is bounded extrapolation of an already-improving voltage/
angle correction: test a few larger steps along that direction, preserve the
original candidate unless the full physical check improves, and leave source
constraints, original base anchor and final economic/security checks unchanged.
This is a proposal, not an implemented or measured improvement. Tiny tests
must establish incumbent preservation, bounds, no discrete-control corruption
and independent full validation before another registered cold experiment.

Archive SHA256:
`a298984ac55d7c3f27bcec7f946f2bcf59522b9cb3deaf677ab3ac7ecdd5a090`.
After matching archived/current status hashes and verifying no active runner,
native solver or reparse points under the exact target, 7,424 disposable
solution-text entries were pruned. Physical recovery was 6,743,203,840 bytes,
leaving 45,310,812,160 bytes free. Logs, base JSON, partial official details,
source and code remain; the deleted vectors cannot be re-evaluated. The small
interrupted V11 attempt and V4's complete verified payload remain intact.

All five scenario top-five gates remain open. V11 has not been run on the
other four scenarios and is not promoted as a complete successful algorithm.

## V12 registered experiment: bounded voltage/angle extrapolation

The new optional probe retains V11 and targets already-improving but modest
corrections, not another fresh factorization. For a branch outage at predictor
iteration two or later, it activates only if the chosen next residual is still
above the unchanged validation tolerance, is strictly better than the current
residual, and has not yet halved that residual. Ordinary options default off;
the registered cached-economic study mode enables the probe.

From the current reference x and the existing selected voltage/angle point y,
the three trials use x + a(y - x), with a in {2, 4, 8}. The ray stays fixed
during the search even when an earlier trial wins. Voltage magnitudes are
clipped to the exact source bounds. Angles are checked by the existing physical
validator. Generator, load, commitment, shunt-step and other controls are copied,
not extrapolated; the existing trial projection still enforces their original
source bounds and original-base corrective limits. A trial replaces the
incumbent only when its checked finite residual is strictly smaller. Rejected
or nonfinite trials cannot lose the incumbent, and no prior-run state is used.

These are internal candidate probes, not new feasibility declarations. The
unchanged full reconstruction/independent checker and official evaluator still
decide whether an outage and the complete scenario pass. No tolerance, cost
curve, PMIN, source limit, contingency list or economic objective is relaxed.
Each eligible iteration has at most three additional probes under the same
existing process/global deadline; searches, trials, selections, elapsed time,
largest selected scale and before/after residuals are serialized in both full
and compact worker summaries.

Tiny arithmetic/selection tests cover the activation rule, fixed-ray geometry,
negative angle direction, clamping, rejection and nonfinite-result preservation,
zero-direction skipping, invalid dimensions and unchanged discrete/other
controls. A separate two-bus outage fixture deliberately perturbs voltages and
angles, then requires the actual independent source checker to verify its
recovered state against the unchanged original base. This physical test is
distinct from the scalar merit-function test of selection arithmetic.

All four native CTest groups and 72 Python tests passed after the final build.
The seven new numeric diagnostics are required nonzero in both serializers by
the tiny serialization test and included in the worker whitelist; actual
worker-log survival will be checked after the full experiment.

The same five scenarios remain registered, with the same 300-second total,
10+40-second primary refinement, exact Hessian and disabled shared-reference
stage. First full V12 experiment: one cold scenario-095 run. No speed or
top-five benefit is assumed from the component-test results.

## V12 cold result: better coverage, still no complete score

Revision `07564bf7ae760aada3e4e3cadcc95eb090a287ad` stopped at 295.333988800
seconds with 4,548/6,579 accepted completed contingencies. The final error was
`fast screen CTG_005795 finished after the end-to-end work deadline`. All source
hashes match. There is no complete official objective or exhaustive certificate.
Coverage increased by 781 contingencies versus V11, but neither incomplete
single run establishes a successful full algorithm or a general speedup.

The base stage took 52.815086208 seconds and selected intermediate 56, native
objective 216129.241361514, independently verified maximum residual
1.1597851425137634e-15. The AC stage took 42.472095256 seconds despite its
40-second requested cooperative budget, with 59 iterations. The hard global
deadline remained enforced. This slight base-time difference is included in
the end-to-end comparison; it is not subtracted to make V12 look faster.

All seven new fields survived in all 4,557 terminal records, including nine
late records. There were 2,157 eligible searches, 6,303 trial checks and 931
selected extrapolations. Of 713 contingencies with a search, 614 selected at
least one larger step and 99 selected none. No recorded before/after pair
lost its incumbent residual. Largest selected scale by contingency was 2x in
54 cases, 4x in 221 and 8x in 339.

Summed worker solve-call time was 5388.318430013 seconds; median call time was
0.444091753 seconds. Extrapolation used 107.424682833 summed seconds. Fixed
Jacobian preparation used 566.908329057 and economic polish 1256.041596025
seconds. Adaptive refresh now made 102 probes with 15 selections, using
93.284546541 seconds. These are parallel-task aggregates with nested phases,
not independent end-to-end times and not additive components.

| Terminal-task group | Tasks | Summed solve seconds | Summed ray-search seconds |
|---|---:|---:|---:|
| No ray search | 3,844 | 2829.146223 | 0 |
| Ray search, none selected | 99 | 237.872961 | 10.888804 |
| At least one larger step selected | 614 | 2321.299245 | 96.535879 |

All 3,773 V11 terminal labels were also observed in V12. For these labels,
summed solve time changed from 5484.494689551 to 5018.141671336 seconds and
predictor iterations from 10,934 to 8,773. Scheduling differs and base iterates
are very close but not bit-identical; these are descriptive matched-label
observations, not controlled repeated timing samples.

| Contingency | V11 iterations | V12 iterations | V11 solve seconds | V12 solve seconds |
|---|---:|---:|---:|---:|
| CTG_004464 | 48 | 12 | 39.631759 | 10.954675 |
| CTG_000673 | 27 | 12 | 31.318806 | 7.981393 |
| CTG_006288 | 49 | 48 | 42.285175 | 42.927170 |
| CTG_000645 | 13 | 24 | 5.550891 | 17.818868 |

The largest pair still rejected almost every proposed larger step: each had
44 searches/132 trials with just one selected 2x step. A better local residual
does not guarantee fewer future iterations, as CTG_000645 demonstrates. V12
helps many cases but does not solve the long tail or establish top-five scores.

Archive SHA256:
`34c659bc03f02e45cb2e7e60cd8dd4685acb9877460f1894048572e8425883f4`.
After checking the archive/current status hashes, terminal processes, exact
target containment and absence of reparse points, 9,113 disposable solution
text entries were pruned. Physical recovery was 8,143,753,216 bytes, leaving
45,143,662,592 bytes free. Logs, base JSON, partial official details, source,
code and V4's full verified payload remain. Deleted V12 vectors cannot be
independently re-evaluated. All five top-five gates remain open.

## Next candidate audit: exact equivalent parallel-branch outages

This is a read-only source/timing audit, not an implemented cache or a new solve.
The worst pair CTG_006287/CTG_006288 removes source branches 32486/32487: parallel
transformers whose complete normalized raw branch records are equal after
excluding only `source_id` and `index`. Their orientations, electrical data,
ratings, status and all remaining metadata are exactly equal; no approximate
electrical matching or tolerance-based grouping was used.

Applying the same conservative raw-record comparison across all five cases:

| Scenario | Branch outages | Generator outages | Distinct branch signatures | Multi-outage groups | Repeats beyond one per signature |
|---|---:|---:|---:|---:|---:|
| 006 | 6,209 | 484 | 5,528 | 672 | 681 |
| 010 | 6,209 | 484 | 5,528 | 672 | 681 |
| 069 | 6,156 | 464 | 5,482 | 664 | 674 |
| 077 | 6,123 | 461 | 5,458 | 655 | 665 |
| 095 | 6,118 | 461 | 5,454 | 654 | 664 |

Among V12's observed terminal tasks, 392 such groups included more than one
completed outage, accounting for 401 repeated tasks. Summing each group's
observed solve times minus its slowest member yields 469.827707007 seconds of
repeated worker work. This is only a work-opportunity estimate: it omits the
cost of reconstruction/verification, excludes unobserved tasks, and is not a
predicted wall-time saving. The worst identical pair alone repeats about 42.2
worker-seconds; a triple CTG_003513/3514/3515 repeats about 26.0 seconds.

Current economic mode deliberately bypasses the ordinary rolling-seed shortcut:
an unrelated feasible seed can carry expensive imbalance into another outage.
It still consults the seed bank after a fresh predictor fails. Exact electrical
equivalence is a different opportunity from indiscriminate warm seeding.
A proposed implementation would route exact equivalent peers to the same
resident worker, reuse only a result produced earlier in this same cold run,
rebuild the requested branch outage, and independently validate every peer
against the original base. A failed reuse check must fall back to the ordinary
solve. Every source label still requires its own solution and official check;
none may be omitted or declared feasible from fingerprint equality alone.

Before a full experiment, tiny tests must prove equivalence covers every
model-relevant branch attribute, changed ratings/reactance/taps/phase/status
break equivalence, cross-outage flows and identities are rebuilt correctly,
the original base anchor remains unchanged, and invalid candidates fall back.
Memory must remain bounded and group scheduling must not duplicate or lose
labels. No prior-run solution or different scenario may seed the cold base.

## V13 registered correction: exact parallel-outage reuse

V13 implements the audited same-run opportunity. Raw fingerprints exclude only
`source_id` and `index`; no numerical rounding or approximate matching is used.
Exact peers stay on the same resident worker after a successful solve. The
native implementation independently compares every model-relevant `Branch`
field, omitting only the three circuit-identity fields. A matching candidate
comes from the existing bounded 16-entry seed bank; no new state cache grows
with the contingency count. Source and different-scenario solutions are not
inputs. The first peer still starts through the ordinary cold-run pathway.

Every subsequent peer rebuilds its own outage, branch flows and derived
fields, checks the original base-dependent corrective constraints, and must
pass the full independent native validator. A rejected candidate uses the
ordinary solver. A failed group can still split and use the existing fallback
queue. Every label retains a separate solution file and official evaluation;
fingerprint equality alone never certifies feasibility or permits omission.
An accepted exact peer does not repeat economic polishing: its equivalent
first peer was already solved within this run. Native diagnostics distinguish
requested, found, model-matched, attempted and accepted reuse and its screen
time, including rejected/fallback cases.

Component evidence before the full run: all 80 Python tests pass; all four
native CTest groups pass. The new tests cover all native branch model fields,
changed raw attributes, correct outaged-flow identity and objective rebuilding,
an unchanged original base anchor, a deliberately invalid candidate falling
back, a changed-rating nonmatch, and successful/failed-group queue coverage
without duplicate or lost labels. These tiny-fixture results are not evidence
that the large case finishes or reaches top five.

Registered config: `config/c2fen19402_top5_exact_parallel_v13.json`. All five
scenarios and their target scores remain registered. Budgets, source data,
root procedure, tolerances and official evaluator are unchanged from V12.
The first full V13 experiment is one cold scenario-095 run. No algorithm edit
is permitted while it is active; all returned reuse diagnostics and the full
label/score/deadline gates will be audited afterward.

## V13 cold result: reuse verified, coverage improved, deadline still missed

Frozen revision `594ba1fec0d7945caac4ed2d485e3a408cd5101a` ran scenario 095
once from unchanged source input. It stopped at 295.298520500 seconds with
5,443/6,579 accepted completed contingencies. The controller reported
`fast screen CTG_006480 finished after the end-to-end work deadline`.
There is no complete official score or exhaustive security certificate.
No other scenario was launched for V13.

| Version | Completed contingencies | End-to-end seconds | Complete official score |
|---|---:|---:|---:|
| Retained V4 | 6,579/6,579 | 259.334549 | 341,534.900354 |
| V12 | 4,548/6,579 | 295.333989 | Not available: timeout |
| V13 | 5,443/6,579 | 295.298521 | Not available: timeout |

V13 completed 895 more contingencies than V12. Both are single censored runs,
not repeated controlled timing samples. The root procedure was unchanged but
its time-bounded selected numerical point differed: V13's base stage took
50.775097764 seconds, selected intermediate 56 with native objective
215855.0930171393, and passed at maximum residual 1.0166706251629788e-15.
The AC stage used 40.445444048 seconds and 59 iterations. This differs from
V12's native 216129.241361514, so whole-run effects cannot be attributed solely
to the reuse change. Native objectives are not the official competition score.

There are 5,459 unique terminal worker records, including 16 that were not
accepted before the deadline. Of 1,004 requests allowing exact-peer reuse,
505 found a prior peer. All 505 passed the native model comparison and direct
screen. Their largest validation residual was 9.925010981237659e-6. A separate
log/source audit confirmed every seed preceded its consumer on the same worker,
both labels were exact raw-record matches, and each successful peer wrote its
own solution. There were zero provenance/signature errors. The largest native
objective difference between a reused peer and its seed was 6.984919309616089e-10.
The existing bank never exceeded 16 entries.

The 505 reuse calls took 20.127268445 summed worker-seconds, median
0.039799948 and maximum 0.056660477. Their direct screens accounted for
19.213500281 seconds. The hard identical pair illustrates the avoided search:
CTG_006287 used 48 predictor iterations and 42.638330405 seconds; CTG_006288
reused it in 0.037706482 seconds, with the same native objective and residual.
Its V12 solve had taken 42.927170265 seconds. This is a local measured example,
not a claim that the complete scenario is that many times faster.

Observed terminal methods: 4,914 economic-polish results, 505 exact-peer direct
screens, 34 fixed-Jacobian predictor results and six bounded Newton rescues.
Total solve-call time summed over parallel workers was 5402.759683711 seconds.
For 4,556 labels shared with V12, the summed time was 5372.492412178 in V12
and 4953.404987265 in V13; one V12 terminal label was not observed in V13.
Scheduling and base-point differences apply to this matched comparison too.

The remaining long tail is measurable: 148 tasks with more than ten predictor
iterations used 1718.763019968 summed seconds. Across all tasks, preparation
used 541.565640317, economic polish 1344.153202764, adaptive refresh
73.547479394 and voltage extrapolation 104.997404785 seconds. These phases
are nested/parallel aggregates and must not be added as independent wall time.

Partial official details cover 4,697 unique labels including BASECASE, with
zero observed infeasibility. They include 411 of the 505 reused peers; 94
reused peers lack an official detail at cutoff. This partial audit is not
the required complete 6,580-label certificate. The retained V4 certificate
still records 6,580/6,580 labels, infeasibility zero, maximum independent
contingency residual 9.643973418016483e-6, and all 6,580 root solution files
remain present. Its 341534.9003543365 score remains below scenario 095's
434319.522756719 top-five target. All five top-five gates remain open.

V13 archive SHA256:
`8615a0836f3c0ee6a52bded555ad9341030f2f16517c531e76dc52a0280d39aa`.
Status SHA256:
`d257ba6d5e12825f1fff4b31ded0bda2895c5145a70dd482e79095654d0677cd`.
After verifying those hashes, terminal controller/native process states,
exact target containment and absence of reparse points, 10,922 disposable
solution-text entries were removed. Physical space recovered was 9,753,190,400
bytes, leaving 44,965,650,432 bytes free. No V13 solution-text file remains.
Logs, base JSON, partial official details, input data, code and V4's verified
full payload remain; the pruned V13 vectors cannot be re-evaluated.

### Next implementation audit

The correction loop copies a complete `AcState` for each trial and then
immediately overwrites its branch-flow and slack fields. Most such candidates
are rejected by the physical checker without consuming the copied PWL weights.
For this base state, those derived vectors contain 273,730 doubles, while
physical-control vectors contain 54,295 doubles plus shunt step vectors.
This is an avoidable-copy hypothesis, not a measured timing result or an
implemented optimization. A controls-only trial construction must be tested
against the existing full-copy pathway, including shunt changes, rejected
candidates, complete final PWL/Ohm rebuilding and original-base constraints,
before a new frozen full experiment. Source physics and score gates must stay
unchanged. Improving runtime alone does not establish any top-five score.

## V14 registered correction: controls-only trial construction

V14 retains V13's exact-peer policy and changes only how a correction trial is
copied. In opt-in cached-economic mode, trial creation copies every voltage,
angle, generator/load control, shunt value/step and commitment/startup/shutdown
field. It does not copy flows, soft slacks or PWL weights that the existing
projection/economic pathway rebuilds before its corresponding checks. All
candidate directions, damping values, selection tests, source bounds and
final independent acceptance gates are unchanged. A rejected candidate remains
unaccepted, and unsuccessful-screen fallbacks still rebuild their complete
derived state before serialization. No base optimization or source data changes.

The old full-copy helper mode remains available as a tiny-test oracle. Tests
compare staged physical rejection, full economic/Ohm reconstruction, exact
objectives and all state fields on branch and generator outages. Fixtures
include changed PMIN, generator/load/voltage/rating violations, valid and
invalid discrete shunt steps and nonfinite voltage controls. A complete tiny
corrective solve and an actual validated extrapolation compare old/new paths,
not just helper arithmetic. The new shunt fixture initially crashed because
source-default controls were implicit; the fixture now materializes them
before its deliberate mutations and uses checked indexing. This was detected
before any full run; no solver gate was weakened to make the test pass.

Four compact runtime fields count total trial copies, controls-only copies,
cumulative derived payload bytes not copied, and actual copy time. Both native
serializers and the worker log whitelist carry these fields. The byte counter
is cumulative avoided copy traffic, not freed RAM or a measured time saving.

Config `config/c2fen19402_top5_control_copy_v14.json` retains all five scenarios
and the same source/target/timing configuration as V13. The first experiment
will be one frozen cold scenario-095 run after all component checks pass.
There is no repeated timing sample and no change while the run is active.

V14 pre-run gates passed: all four native CTest groups and all 80 Python tests.
The complete tiny solve also matched trial-copy counts between old/new paths.
The registered JSON differs from V13 only in its description; the behavior
change is pinned by the compiled code revision and executable hash.

## V14 cold result: 13 contingencies short of completion

Revision `e1208ba9f62b942668b9de09d20daa256d3d45b2` ran scenario 095 once.
The total was 299.475812200 seconds, with 6,566/6,579 accepted completed
contingencies. The work-deadline error was
`fast screen CTG_006376 finished after the end-to-end work deadline`.
Process cleanup is included in the total; the run remains a timeout with no
complete score or exhaustive certificate, despite completing 1,123 more
contingencies than V13. There are 6,567 unique terminal worker records and
6,568 root solution files including BASECASE, so twelve contingency solutions
were never written and one terminal result was not accepted before cutoff.

All four copy metrics survived in all 6,567 worker records. They record 216,112
trial copies, all controls-only, avoiding 382,995,152,152 cumulative payload
bytes. Copy time itself was 55.378701409 summed worker-seconds. This is avoided
traffic, not freed memory; there is no measured old-path copy-time baseline.
The maximum native residual among returned records was 9.993642045180717e-6.
The 662 exact-peer screens still worked. Returned methods were 5,869 economic
polishes, 662 exact peers, 32 predictors and four bounded Newton rescues.

Total reported solve time summed over workers was 5339.880551487 seconds,
median 0.273903585. All 5,459 V13 terminal labels were also observed: their
summed solve time was 5402.759683711 in V13 versus 4987.507818219 in V14.
This is a single-run descriptive comparison, not a repeated controlled
speedup measurement. The time-bounded base points were not bit-identical:
V14 selected intermediate 56 with native objective 216129.2413970343 and
maximum residual 1.7763568394002505e-15, using 50.761182672 base-stage seconds.
The official base-only objective was 214720.19387340322.

Reported phase aggregates: preparation 557.855387128, economic polish
1418.530383277, adaptive refresh 79.787802693 and extrapolation 95.116115457
seconds. There were 5,901 polish attempts and 11,663 polish iterations.
These are nested parallel aggregates from retained results, not additive wall
times or an exhaustive accounting of any earlier result replaced by rescue.
Preparation was not predominantly transformer work: 510 line-outage records
accounted for 402.788646259 seconds, 108 transformer records for 88.849433337,
and 89 generator records for 66.217307532.

Partial official evaluation contains 5,915 unique detail labels including
BASECASE, all reporting infeasibility false. It covers 561 of the 662 reused
peers; 101 peers have no official detail before cutoff. The partial contingency
mean was 134886.0224503414, but the unevaluated set is nonrandom and this must
not be extrapolated into a complete score. Corrective economic quality still
needs improvement; a runtime pass alone will not establish top-five results.
All five scenario targets remain open and no other scenario ran in V14.

V14 archive SHA256:
`9d49923fbab2588700cf9ef4311184ea78711181e5282af3d897ff37c3009302`.
Status SHA256:
`1d3d82536812d039a58d87d23da141d175937f415e12908132bc7a7417a60346`.
After verifying archive/status hashes, terminal controllers/native workers,
target containment and no reparse points, 13,144 disposable solution-text
entries were pruned. Physical recovery was 11,729,641,472 bytes, leaving
44,754,944,000 bytes free. Logs, base JSON, partial evaluator details, all
source/code and V4's complete verified payload remain. Deleted V14 vectors
cannot be independently re-evaluated.

### Next measured-work target

Economic polish currently constructs a fresh sparse `YRows` admittance map
for each attempted outage and recomputes complex network injections at every
iteration. At that point `polished_state` already contains freshly rebuilt,
independently checked branch flows. Summing those terminal flows plus the
current shunt injections is the same AC injection equation and could remove
the redundant map construction and trigonometric work. This is a proposed
optimization, not yet a measured saving. It requires numerical comparisons
against the existing complex-admittance calculation including phase-shifting
transformers, taps, terminal shunts, outages and changed discrete shunt controls,
plus complete tiny-solve validation before another frozen full run.

## V15 registered correction: validated-flow injection reuse

V15 retains V14's cold procedure, exact-peer routing, controls-only trial
copies and all source constraints. Economic polish now obtains each bus's
AC network injection by summing the terminal flows of its in-service,
non-outaged branches and adding the current shunt injection. Those flows
belong to the complete, freshly rebuilt and validated polished incumbent,
not to a controls-only trial or a stale prior-run state. This removes the
polish-only admittance-map construction and repeated trigonometric injection
calculation. It does not remove branch-flow variables, modify any optimization
row, substitute a linear network, or skip any final feasibility/Ohm/PWL check.
The old complex-admittance calculation remains a component-test oracle.

The two-bus equation regression covers fifteen variant/outage combinations:
parallel lines, a phase-shifting transformer, non-unit taps, terminal shunts,
an offline branch, and explicit/implicit changed discrete-shunt controls.
Terminal-flow sums must agree with the complex-admittance oracle within
1e-11 p.u. Malformed flow dimensions must throw, and the source base is
unchanged. A complete tiny corrective solve compares both paths, including
objective, reconstructed state, discrete controls and independent validation
against the original base. Nonfinite comparison values are rejected.
The old/new state and objective comparison tolerances are 1e-10 and 1e-6,
respectively; production feasibility tolerances are unchanged.

Four runtime fields record injection reuse count, polish Y-bus build count,
injection seconds and Y-bus seconds. Nonzero serializer tests cover both
compact and full result JSON, and the worker whitelist preserves these fields.
These are measurements of the executed path, not a claimed speedup.

Config `config/c2fen19402_top5_flow_injections_v15.json` keeps all five
scenarios, the same 300-second deadline and the same official targets as V14.
Only its description changes; the implementation is pinned by the frozen
revision and executable hash. After component gates, run scenario 095 once
from source to measure complete coverage and economic quality. No repeats,
prior-run initializations, or edits during that experiment are permitted.

V15 pre-run gates passed: all four native CTest groups and all 80 Python
tests. The equation oracle and full tiny-solve comparisons above passed in
the compiled native test, including the new nonzero metric serialization and
independent complex-path checks.

## V15 deliberately stopped: missed decoupled fallback

The single cold V15 run used `306b6f5135ec968fec0e218cb8e80eb3f317c9c2`.
During read-only review of the active experiment, an uncovered fallback was
found: when the coupled polish correction fails, the existing decoupled
correction recomputes injections after changing angles. V15 could pass an
empty polish Y-bus into that calculation because it skipped the initial
construction. The accepted incumbent's flows cannot be reused after that
angle update either. This was a code-inspection finding, not an observed
source infeasibility or proof that the faulty branch executed in the run.

The experiment was intentionally stopped by sending TERM to one positively
identified worker and letting the controller abort and finalize all workers.
Session 94427 ended with return code 1; native workers were confirmed absent.
Recorded total: 90.09472399999504 seconds, 149/6579 accepted contingencies,
and no final objective or exhaustive certificate. The recorded error is
`fast-screen worker 3 exited with status 15`, caused by the deliberate TERM.
There was no algorithm edit while the process was active. This is an aborted
experiment, not a deadline failure, and it cannot qualify toward top five.

The verified compact evidence is
`docs/evidence/C2FEN19402_TOP5_V15_ABORTED_20260915.json`, SHA256
`8844a05ed9976dd59c0533f2174784b77d75a059a1f28c27fef92526c1a913ca`.
The small partial payload, logs, base state and official details remain.

## V16 registered correction: lazy, tested fallback injections

V16 retains branch-flow reuse for the unchanged accepted point. On a
decoupled angle update it now constructs the admittance map lazily if needed
and computes injections from the updated raw-trial voltage and angle, not
from stale derived fields. The map is local to one fixed-topology,
fixed-shunt polish; subsequent corrections reuse it. Production still tries
the coupled correction first. A C++ option permits the tiny regression to
exercise the pre-existing decoupled path directly; the registered production
configuration does not change that choice. Invalid injection dimensions now
throw instead of allowing an out-of-range access.

The equation fixture now compares two changed-voltage fallback injections
for each of the fifteen variant/outage combinations with an independently
fresh complex-admittance calculation, requiring exact equality and exactly
one lazy construction. It uses controls-only trials, so the test cannot
accidentally consume prior flow arrays. Empty matrices are explicitly rejected.
Two complete tiny solves compare forced-decoupled mode with and without
flow reuse, check the matrix-build/reuse counters, compare objective and all
reconstructed controls/state, and independently validate both against the
original base. The original coupled comparisons also remain.

V16 component gates passed: all four native CTest groups and all 80 Python
tests. Config `config/c2fen19402_top5_lazy_fallback_v16.json` retains all five
scenarios, unchanged source constraints, 300 seconds and official targets.
Run one new cold scenario-095 experiment after freezing/pushing; it replaces
the abandoned faulty candidate, not a repeated sample of V15. All five
top-five targets remain unmet.

## V16 fully verified result: new study incumbent, still below top five

The single cold run of `9ebc66343d2c939ff7c72026de15232dccc4cbbe`
completed all 6,579 source contingencies and official evaluation in
297.7028850999923 seconds. The controller returned zero with no failed gates.
The raw official objective is 356907.86098102527, a gain of
15372.960626688786 over V4. It remains 77411.6617756937 below the scenario-095
fifth-place target. No other scenario ran with V16, and this is not completion
of the five-scenario goal or a global optimality certificate.

| Scenario 095 | Seconds | Official objective | Complete / feasible / deadline | Top five |
|---|---:|---:|---|---|
| V4 `8a6886c` | 259.334549000 | 341534.900354337 | PASS | No |
| V16 `9ebc663` | 297.702885100 | 356907.860981025 | PASS | No |
| Published fifth-best target | -- | 434319.522756719 | Comparator | -- |

The post-run audit reread the raw CON label set and all 6,626 physical official
detail records, including repeated identical BASECASE details across shards.
There are exactly 6,580 unique labels matching source contingencies plus base,
with no infeasible record. Reconstructing base objective plus mean contingency
objective reproduced the reported score exactly. Source/model/executable hashes
match the frozen manifest, and all 6,580 root solution text files are retained.
Base independent residual is 8.881784197001252e-16 and maximum independent
contingency residual is 9.993580626088683e-6. Official infeasibility is zero.
Feasibility is under the unchanged GO2 model, which allows penalized bounded
imbalances; it does not mean zero physical imbalance in every contingency.

V16 reported 11,678 flow-injection reuses and zero polish Y-bus constructions;
thus its production run did not exercise the repaired decoupled fallback.
That path was exercised by the complete tiny tests, not inferred from this run.
There are 5,879 economic-polish, 664 exact-peer, 32 predictor and four bounded
Newton-rescue terminal records. The exact-peer/source-label requirements remain.

Summed parallel phase times: polish 1192.351667995 seconds, correction
194.177659862, injections 19.746578682, physical trial checks 611.849160367,
economic checks 112.104251405, trial copies 56.768237306 and predictor
preparation 561.940502108. These are nested worker aggregates, not additive
end-to-end times. The 6,567 labels shared with V14 used 5339.880551487 summed
worker seconds there versus 5140.734593703 here. This is not a controlled
repeated speedup estimate; V14 did not finish and the complete V16 score is
the meaningful new outcome.

### Remaining economic gap

| Official component | Base | Mean contingency |
|---|---:|---:|
| Load benefit | 336344.135709104 | 336341.338992253 |
| Generator cost | 119373.366179767 | 119380.353986168 |
| Active imbalance penalty | 302.659891775 | 30044.382454334 |
| Reactive imbalance penalty | 198.063511601 | 43600.055869567 |
| Line penalty | 1749.852342079 | 1128.879485042 |
| Transformer penalty | 0 | 0 |
| Objective | 214720.193783882 | 142187.667197143 |

The aggregate bus penalty is the largest remaining loss. Of 33,653 polish
trials, 19,422 failed a physical check before economic reconstruction.
The 2,491 tasks that used all three polish iterations contribute
57903.000327297035 to the overall mean bus penalty of 73644.4383239012.
This points toward improving the accepted correction direction and allocation
of work, not merely increasing iterations globally. The current compact logs
do not identify the limiting physical category for every rejected trial;
do not assume that all rejections are thermal, voltage, or balance violations.

Holding observed dispatch/load benefits and generation costs fixed and
hypothetically removing every bus/thermal penalty yields 433931.7545354227,
still 387.7682212963 below the target. This is an accounting counterfactual,
not a feasible candidate or global bound. More efficient imbalance cleanup
and better genuine dispatch economics are both required. The next algorithm
change must retain exact source constraints and complete independent checks.

V16 full vectors are the new retained study incumbent. Compact archive SHA256:
`4bcd23cb20a3d2d42cdc4a86b49f20616da407a378f0e8e867b9f47d94d3b0aa`.
Status SHA256:
`3663703dcc2f08b95c06d858433ef5a0362628d2c4e2e50dc8085a417920698b`.
The independent scalar audit and component breakdown are preserved in
`docs/evidence/C2FEN19402_TOP5_V16_AUDIT_20260915.json`.

### Incumbent retention and storage recovery

After the complete V16 audit, V4's older solution-text payload was pruned.
The V4 archive hash `382791cc4a0971b267da5aa3fdd6299579f01d135d1a68e0529b2a00b03cc1e1`
and status hash `f396e1bcf8c61ea2d6e23ac138e91d680eb926adbf437f9fcc6026470bfeb984`
were checked, alongside V16's replacement archive/status. The exact target
was contained within the local campaign, with no ancestral or subtree reparse
points. Windows controllers and native WSL workers were confirmed absent.

Only `v4_s095/C2FEN19402_s095_cold/**/solution_*.txt` was removed: 13,206
file entries, including hard links. Physical recovery was 11,671,105,536
bytes, leaving 43,905,077,248 bytes free immediately afterward. No V4 solution
text remains. Its base JSON, logs, official details/certificate, code and source
inputs remain, but the deleted V4 vectors cannot be independently re-evaluated.
All 6,580 V16 root solution files remain for future re-evaluation. No source
data, environment, algorithm, active run, or other scenario's payload was deleted.

## V17 registered correction: fixed-network local economic dispatch

V16's evidence identifies mean corrective imbalance penalties of 73644.44.
The initial predictor's local controls mainly target entry into the permitted
0.5-p.u. imbalance band. V17 adds a separate economic candidate pass after a
verified predictor point and before its cached voltage/angle polish. During
this new pass, network voltages, angles, branch flows, topology, shunts and
commitment are fixed. Only generator P/Q and source-flexible load factors may
move, within the original-base corrective ramp intervals and exact source
PMIN/PMAX/Q and load bounds. It is neither a new network model nor removal of
any branch variable or physical constraint.

For one active generator's P coordinate, the local minimized function is its
unchanged PWL cost plus the active imbalance penalty at that bus. For one load
factor it is negative source PWL benefit plus both active and reactive
imbalance penalties, preserving the load's specified P/Q ratio. Every kink,
source-bound endpoint and zero-imbalance point is considered. This is an
exact one-dimensional PWL coordinate minimization even with nonconvex source
slopes, not a global solution of the network or even the joint bus subproblem.
The existing native linear imbalance penalty coefficients are used unchanged;
the final score still comes exclusively from the official evaluator.

Qg has no variable production cost in this formulation. Each active Qg can
reduce its local reactive mismatch within the exact Q bounds. At most two
deterministic coordinate sweeps are performed. Both bus imbalance limits are
respected when considering each coupled load movement. Generator/load ramps
are always anchored to the immutable base, not the current candidate.
Offline/outaged units remain untouched. The PWL represented power interval
is also respected; no source curve is convexified or replaced.

The resulting complete state is rebuilt and independently checked against
the original base. Only a finite strict native-objective improvement can
replace the incumbent. Any rejected candidate leaves that incumbent intact.
The normal cached correction then continues, unless the unchanged existing
aggregate-slack work target is already reached; this skips unnecessary work,
not feasibility checks. The legacy pathway remains available with the new
C++ option off, and the existing V14/V16 injection/copy/fallback oracle tests
explicitly retain that old pathway.

Component tests compare kink enumeration to dense one-dimensional oracles
for convex/nonconvex curves and signed P/Q effects, exercise a zero-active-
power reactive coordinate, exact positive PMIN and both Q bounds, original-
base ramps distinct from the candidate, offline/outaged generators, network
and base immutability, and nonfinite rejection. A full tiny corrective solve
must select a locally improved candidate, pass the independent AC/PWL checker,
and match its predicted local gain to the actual rebuilt objective gain.
Ten compact fields retain attempts, selected outcomes, control-change counts,
time, objective before/after, predicted gain and rejection category.

Config `config/c2fen19402_top5_local_dispatch_v17.json` retains all five
scenarios and the same source/target/300-second contract as V16. The first
experiment is one frozen cold scenario-095 run after all component gates.
V16's complete solutions remain an audit/comparison artifact only and cannot
initialize this run. No algorithm edits or repeats during the full experiment.

V17 pre-run gates passed: all four native CTest groups and all 80 Python
tests after the final build. The registered JSON differs from V16 only in
its description. The implementation and native binary, not a changed source
case or prior solution, define the new candidate-generation policy.

## V17 cold result: economic benefit, deadline failure

Revision `0e67268bde491487207661fb719ad274b630c41b` ran scenario 095 once.
It stopped at the reserved work deadline with 6,477/6,579 contingencies
completed, 102 short. Total including finalization was 296.48433989999467
seconds. The recorded error was `fast-screen worker 4 reached the end-to-end
work deadline`. The controller returned 1 and native workers were confirmed
absent. There is no complete official score or exhaustive certificate, so
V16 remains the retained fully verified study incumbent. No other scenario
ran with V17, and no five-scenario top-five target was closed.

The base used 51.102870572 seconds. Its unchanged sparse AC policy again
selected intermediate 56, native objective 216129.24138905777; the AC stage
used 40.766033045 seconds and 59 iterations before its own time limit. Source
hashes match the previous cold runs. There was no prior-run initialization.

All 6,477 unique terminal worker records preserve the new local-dispatch
metrics. The pass was attempted and selected in 5,825 records, with no
reported rejection. Summed predicted native gain was 254971277.43702352,
versus 254971277.43692136 when rebuilt. That sum is across individual
contingencies, not a scenario score. The remaining terminal methods were
648 exact-peer screens and four bounded Newton rescues. Maximum reported
native residual was 9.989966141821238e-6. There were 6,499 root solution-file
entries at cutoff, more than the accepted terminal count; those extra files
must not be mistaken for additional in-deadline accepted contingencies.

Local dispatch consumed 203.125703083 summed worker seconds. It made
2,252,875 Pg changes, 2,175,046 Qg changes and 71,076,781 load-factor changes
across its bounded sweeps. Total polish time, including this pass, was
1478.136497 seconds; physical checks were 678.212627999 and predictor
preparation 559.086355631 seconds. These are nested parallel aggregates, not
additive wall times. There were 11,913 polish iterations, 36,932 trials and
21,396 physical trial rejections. The iteration-count histogram was 783
records at zero, 2,228 at one, 713 at two and 2,753 at three. The local pass
therefore did not simply replace an equivalent amount of later work.

On the 6,477 labels shared with V16, summed worker time rose from
5112.537167592 to 5413.003530535 seconds. Mean native final objective improved
from 144528.5387521601 to 161049.494131731. These are matched diagnostic
comparisons, not repeated timing measurements or official scenario scores.

Partial official evaluation contains 5,290 unique detail labels including
BASECASE, all with infeasibility false. On the 5,289 matching contingency
labels, mean official objective improved from V16's 125927.11513342659 to
146058.93454034152; mean bus penalty fell from 89904.7008699441 to
69781.45260594781. The base-only official objective was 214720.1938620892.
This is a nonrandom incomplete subset. It cannot be extrapolated into a
complete score or substituted for the missing exhaustive certificate.

V17 archive SHA256:
`485805f937098c16fe213d0dcd3a40adb8daaec3b06c44efab7f76ef64b79809`.
Status SHA256:
`3fac8ecac7e3edc1921515190ebacc16b358f4c55729ebf005d86840156aa63f`.
Compact attribution is preserved in
`docs/evidence/C2FEN19402_TOP5_V17_ATTRIBUTION_20260915.json`.

### Next evidence-directed correction

The new local pass both repeats immutable source-bound/PWL preparation for
each outage and changes the right-hand side of the subsequent Newton search.
Its aggregate 203 worker seconds are material, and the later search did not
become cheaper. A next candidate is to cache only immutable per-base source
bounds and cost curves, and apply local cleanup to the verified endpoint of
the original V16 Newton search rather than perturb that search beforehand.
Because local dispatch does not change network controls, already-checked
branch flows can be retained while rebuilding P/Q slacks and PWL economics;
the final full checker must still include Ohm, thermal and every source-bound
check. These are proposed changes, not yet implemented or benchmarked.
They require equivalence tests, source/base immutability tests and a new
frozen revision before another single cold experiment. Merely recovering the
deadline will not establish the missing top-five quality gate.

### V17 evidence retention and pruning

Before pruning, the archive/status hashes above were independently matched;
the exact resolved target remained inside the local campaign and had no
ancestral/subtree reparse points. No experiment controller or native worker
was live. Only V17's `solution_*.txt` payloads were removed: 12,961 entries
including hard links, recovering 11,568,943,104 physical bytes and leaving
43,718,287,360 bytes free immediately afterward. No V17 solution-text file
remains. Base JSON, worker logs, partial official details, status, manifest,
code and source inputs remain; the deleted V17 vectors cannot be re-evaluated.
All 6,580 V16 root solution files remain as the complete verified incumbent.

## V18 registration: endpoint dispatch with source-only caching

V18 implements the correction identified by V17's deadline evidence. It
restores the original V16 Newton/optional linearized polish search, then
applies the same bounded, fixed-network local economic cleanup to that
search's verified endpoint. The per-resident cache contains only source PWL
curves and bounds anchored to the original base of this run; it contains no
prior-run solution or candidate dispatch. The outaged generator remains
excluded at each invocation. The base cannot alias a mutable candidate.

Because cleanup changes only generation and load controls, verified terminal
branch flows and thermal-slack values are retained. Balance slacks and PWL
economic fields are rebuilt, and the full independent checker, including
Ohm laws, thermal constraints and original-base corrective bounds, must
accept a strict objective improvement. Otherwise the previously verified
endpoint is retained. Source physics, PMIN, ramps, costs, tolerances,
contingency coverage and the 300-second deadline are unchanged.

Component gates passed: all four native CTest groups and all 80 Python
tests. New native checks compare resident versus fresh source caches across
generator/branch outages; reject base/candidate aliasing; verify the original
Newton endpoint and iteration count before cleanup; compare retained-flow
fields, residuals and objective against a complete AC rebuild; and test a
second tiny invocation using the cache. Compact logs retain preparation time
and cache-hit evidence. These tiny tests are not full-case repetitions.

Registered configuration:
`config/c2fen19402_top5_endpoint_dispatch_v18.json`. Its only JSON difference
from V17 is the description. The first full experiment is one cold scenario
095 run in `runs/top5_cold_20260915/v18_s095`. The implementation must be
committed and pushed before launch; no changes or repeat while it runs.
This combines search ordering, source-preparation caching and derived-field
reuse; it is not an isolated speedup measurement. No timing or score is
claimed until the run finishes. All five published top-five targets remain
open, and V16's complete solution vectors remain protected.

## V18 cold result: one missing contingency and an input-pipe failure

Frozen revision `3c3dc5d5884f31a7df1e569d13098ac8a77faad5` ran scenario 095
once. The controller stopped after 298.08779429999413 seconds with 6,578 of
6,579 source contingencies recorded. `CTG_004230` is absent from native task
results. There are 6,579 root solution entries including the base. All source,
normalized-model, configuration and native-executable hashes match the run
manifest. The base stage took 50.936386104 seconds.

The exception was Windows `OSError: [Errno 22] Invalid argument` while sending
a new task through `process.stdin.write` at runner line 3912. The controller
returned 1; native workers were confirmed absent. The saved status says
`code2_timed_out=false`, because only CompetitionTimeout exceptions were
classified as timeouts. The original log did not preserve the failing
worker's exit status. A deadline/closed-pipe race is plausible at this time,
but is not proven; this must not be presented as source infeasibility or as
a known native solver crash. There is no complete official score/certificate.

All 6,578 native records have residuals at most 9.993593390100752e-6. Methods:
5,910 economic-polish results, 664 individually checked exact-parallel peers
and four bounded Newton rescues. Local cleanup was selected in all 5,910
attempts with no rejected local candidate; 5,886 cache hits and 24 misses
confirm reuse once per resident worker. Total source-cache preparation was
0.048990192 summed worker seconds. The local phase consumed 157.336205196
worker seconds; total polish including local cleanup was 1345.62695255.
Physical trial checks still consumed 609.030432062 seconds, including 19,421
rejected trials out of 33,651. These parallel/nested values are not additive
end-to-end wall times.

On all 6,578 labels shared with V16, mean native objective increased from
145528.358336857 to 164683.15610404455, while summed worker time increased
from 5141.77962887 to 5265.986489364 seconds. On 6,477 labels shared with V17,
mean native objective increased from 161049.494131731 to 163966.05992509198,
and summed worker time fell from 5413.003530535 to 5235.711649117 seconds.
These are matched diagnostics, not controlled timing repetitions or complete
official-score comparisons.

The partial official archive has 5,900 physical detail files: 5,857 valid
unique labels including BASECASE, 42 equal duplicate base records and one
empty interrupted detail (`shard_040/CTG_004828`). All valid details have zero
infeasibility. On the 5,856 matching contingency labels, mean official
objective increased from V16's 133997.32464488051 to 155278.8061974332, and
mean bus penalty fell from 81834.6216637575 to 60559.238199599706. The base
objective is 214720.19390553056. The remaining subset bus penalty is primarily
reactive (43554.492667983504 versus active 17004.74553161621). This incomplete,
nonrandom set cannot be extrapolated into a full score or top-five result.
V16 remains the complete verified study incumbent; all five targets remain
open, and V18 has not run on the other four scenarios.

Compact provenance archive SHA256:
`c8f6dc296c9d182eea1d8a0c19947d08d4733c439b21b75945d9f6e137ff12a7`.
Status SHA256:
`a5fe89ab854b2ea2134de38115ce7911d3428f21245bf586390f6edccaa28afa`.
Detailed attribution is in
`docs/evidence/C2FEN19402_TOP5_V18_ATTRIBUTION_20260915.json`.

### Worker-input diagnostic correction after V18

The runner now refuses new tasks after the work deadline and sends each
message exactly once. A write or flush failure records the worker, task,
remaining time, OS error and observed process exit code; a bounded observation
of at most 0.05 seconds, within remaining work time, can collect a delayed
exit code. A known timeout exit (124) or actually exhausted deadline is
reported distinctly; another known exit or an unresolved early pipe failure
is not mislabeled. Partial writes are never retried. Only a stop message may
use finalization time. Both fast and corrective worker paths use the helper,
and final worker exit codes are retained in logs. This improves failure
diagnosis; it does not retroactively certify V18 or establish its cause.

All 89 Python tests and all four native CTest groups pass, including nine new
tiny worker-input cases covering success, deadline refusal, timeout exit,
Windows EINVAL, flush failure, unknown early failure, a deadline race, safe
shutdown and prevention of deadline opt-out for tasks. No replacement full
run has been performed for this diagnostic fix.

### Retention and next experiment gate

The attempted guarded deletion of V18's disposable solution-text payloads
was rejected by the execution environment before the command ran. No V18
payload was deleted. Do not retry the deletion by another mechanism to
circumvent that policy. V16's complete incumbent also remains intact. Free
space was about 31.6 billion bytes, below the registered 30 GiB pre-run floor;
another full experiment requires the cleanup restriction to be resolved or
additional permitted local storage. No storage threshold was reduced and no
replacement run was launched.

A source-code inspection identified a candidate for the next runtime change:
economic line-search trials currently finish the physical scan even after a
violation already proves rejection. The existing early-rejection helper is
for merit improvement and uses a 1e-10 comparison cushion; it CANNOT be
reused directly at the feasibility tolerance, because it could return early
with a residual still inside tolerance. A separate strict-greater-than
feasibility rejection mode, with boundary-focused equivalence tests and full
checks for every potentially accepted candidate, is needed before testing
this idea. It has not been implemented or benchmarked. This is intended to
recover time, not to claim that V18 already reaches the published top-five
objective; further score improvement is still necessary.

## V19 component milestone: strict early trial rejection

The proposed strict rejection path is now implemented and component-tested,
but has NOT had a full scenario run. It targets the 19,421 rejected economic
trials observed in V18. That run's 609.030432062 summed worker seconds of
physical checking include rebuilding flows and slacks, which this change
does not skip; those seconds are not a predicted saving or a wall-time bound.

The proof is simple: the maximum residual over the constraints checked so
far cannot exceed the maximum over all constraints. Once the partial maximum
is strictly greater than the unchanged tolerance, this candidate cannot be
accepted, regardless of unvisited constraints. The new API returns both a
report and an explicit early-rejection flag. An early report is only a
rejection witness, never an acceptance certificate or the global maximum
residual. At or below tolerance, the physical scan continues to completion,
then PWL economics and Ohm-law checks are completed just as before. Candidate
construction, backtracking order, selection arithmetic and the final full
independent checker are unchanged. The older 1e-10 merit-improvement shortcut
is unchanged and remains separate.

The option defaults off for ordinary callers and is enabled by the existing
cached-economic study policy. The old complete physical scan remains an
explicit equivalence oracle. Native/compact worker records now include early
rejection counts and the category of the rejection witness. These categories
are not necessarily the largest violations in a fully scanned rejected
candidate. The next full run must establish actual frequency and saved time;
the added diagnostic fields alone prove neither.

The new tiny boundary fixture tests exactly-at-tolerance and adjacent floating
point values on either side, including a first residual inside the old merit
margin followed by a later invalid constraint. It also covers zero tolerance,
invalid tolerances, nonfinite state entries after an earlier violation,
dimension errors and state/base immutability. Existing branch and generator
outage fixtures, including switched shunts, compare the strict path to both
the previous staged scan and the complete rebuild/check. Potentially accepted
states, objectives and every reported residual category are identical. A
complete tiny corrective solve also agrees exactly in dispatch/state,
objective, trial count, iteration count and rejection count when the new
option is enabled versus disabled. Serialization tests cover the new numeric
counter and witness map.

Verification passed after the final native build: all four CTest groups and
all 89 Python tests. This is component evidence, not proof of 19,402-bus
runtime, complete security coverage or a top-five score.

The ready configuration is
`config/c2fen19402_top5_strict_trial_rejection_v19.json`; comparison with V18
must show no JSON change except the description. It retains all five scenario
targets, cold source initialization, exact source constraints, 300 seconds
end-to-end, and the 30 GiB storage floor. The first intended full experiment
is one cold scenario-095 run in `runs/top5_cold_20260915/v19_s095`, after the
storage restriction is resolved. That output directory has not been created.
No cleanup was retried, no safety floor was reduced and no full run started
in this milestone. V16 and V18 solution payloads remain untouched. All five
top-five gates remain open.

## V19 resumption and cold result (2026-09-16)

The user renewed authorization to remove unnecessary old output and continue
the study. Guarded pruning of the five older 6f0afaa post-cleanup runs recovered
about 55 GiB net; the newer complete incumbent sets were preserved. See
`docs/CLEANUP_AND_V19_RESUMPTION_20260916.md`. This was an authorized new cleanup
of specifically audited older targets, not a retry of the earlier blocked V18
deletion. V16 and V18 remain untouched.

V19 ran once, cold, at revision `7acd73d1bf87c867bdd936967e6893fe474f3398`.
The unchanged native binary SHA256 was
`e05958fdd35dc077defd083ab39f67f1eb923cda96ac2d25bacd044eaf84a8c1`.
It recorded all 6,579 source contingencies, but failed after 297.4473472000027
seconds at runner line 4155: fast-screen worker 16 returned timeout exit 124
during the post-task shutdown path. Every fast worker's final log records
124; this is not a reported grid infeasibility. The controller returned 1.
The completed task count does not waive the final verification requirement:
there is no complete official certificate or score, and V16 remains the
complete verified study incumbent.

All 6,579 task-result records are present, with maximum native residual
9.993589281387383e-6. Methods: 5,911 cached economic-polish results, 664 checked
exact-parallel peers and four bounded Newton rescues. The retained official
details contain 5,743 valid unique labels (including base), 42 identical
duplicate base records and one empty interrupted detail. Every valid detail
has zero infeasibility. This incomplete subset is not an exhaustive check.

| Diagnostic | V18 (6,578 tasks) | V19 (6,579 tasks) |
|---|---:|---:|
| Summed solve-call seconds | 5265.986489364 | 5169.492746828 |
| Summed physical trial-check seconds | 609.030432062 | 519.323721736 |
| Summed economic-polish seconds, including local cleanup | 1345.626952550 | 1253.900899712 |
| Summed local dispatch seconds | 157.336205196 | 156.821875555 |
| Rejected physical trials | 19,421 | 19,423 |

These are parallel, nested worker timers, not additive wall times or a
controlled repeated-run speedup claim. All 19,423 V19 physical rejections
used the strict early path. Rejection-witness categories were variable bound
17,607, active balance 1,120, reactive balance 696. Witnesses are not global
maximum residual categories of rejected points. No tolerance was relaxed.

The compact archive is
`docs/evidence/C2FEN19402_TOP5_V19_TIMEOUT_20260916.json`, SHA256
`dbecf1e1d02f4d310f3184d495f6eefcc9ed5943eb40af8897ee9133da8c9405`.
All V19 solution files and diagnostic evidence remain available.

## V20: nonblocking evaluator startup

Inspection found a real orchestration hazard: increasing streaming evaluation
capacity synchronously constructed persistent Python evaluator processes and
waited for their import/ready handshakes while holding the lock needed by
every screen worker's completed-result handoff. Even promotion with no ready
shard started additional processes. V19 did not time these waits separately,
so they are a plausible contributor, not a proven sole cause of that timeout.

V20 registers each process immediately, then waits for its ready handshake
asynchronously without holding the shared lock. No evaluation task is sent
until the handshake future succeeds. Startup failures still abort the run;
an unready process is terminated on abort/deadline, not treated as evaluated.
The initial pool is unchanged; additional processes are started only when
running/queued shards need them, within the existing promoted concurrency cap.
Unused startup readers are terminated rather than racing shutdown reads on
the same pipe. Successful shard outputs still receive the same independent
checks, complete-label verification and official aggregation. Timeouts are
not ignored or reclassified as success.

New diagnostics record each evaluator's startup seconds and each native
result's reporting/evaluation-handoff time. Native binary, candidate generation,
case, source semantics, validation tolerances, CPU worker counts, evaluation
equations, 300-second global deadline and 30 GiB storage floor are unchanged.
The V20 configuration differs from V19 only in description.

Seven new tiny tests exercise nonblocking reporting with delayed readiness,
no task before readiness, fail-closed startup, pending-startup cancellation,
demand-limited promotion, expired-deadline refusal, and real-process exit or
missing-handshake cleanup. All 96 Python tests passed before registration.
Run one cold scenario-095 experiment only after final component checks and
pushing the frozen code, into `runs/top5_cold_20260915/v20_s095`. No V20 score
or runtime benefit is claimed in this registration.

## V20 cold result: native completion; finalization missed its deadline

The registered experiment ran once at frozen revision
`426dce2a06df60c2a2bef02ba5b7aff2e4ab5773`. All four native CTest groups and
96 Python tests passed before launch. The source files, normalized case,
native executable, configuration and revision were checked against the
frozen manifest after completion. No solver configuration changed in flight.

The native stage recorded all 6,579 contingencies, and every one of the
24 native workers exited normally with code 0. Its last result handoff had
4.43273969999791 seconds left in the 295-second work budget. All 47 independent
evaluator shards subsequently returned successful responses. The controller
then reached the 298-second evaluation cutoff waiting for independent shard
finalization futures, with two seconds reserved for final shutdown/output.
Recorded elapsed time was 298.01620340000227 seconds. This is an evaluation
finalization timeout, not native infeasibility or another worker-input error.
It remains a failed five-minute benchmark: the separate post-run audit below
does NOT retroactively pass the timing gate.

The completed solver stage consumed 5165.188301231001 summed worker seconds;
physical trial checks 514.090732923001; local dispatch 153.3668516799999.
Recorded result handoff totaled 66.87422990003324 worker seconds, with maximum
single handoff 0.43347279999943567 seconds. Extra evaluator startup handshakes
took about 1.1-1.3 seconds; their first shard evaluations took about 4.2-7.4
seconds. Those are observed timers, not isolated causal speedup measurements.
No failures are ignored by the new startup path.

### Read-only post-run audit, outside the benchmark

Without launching any new solver or evaluator, the retained details were
reread and checked for exact source-label coverage, duplicate base agreement,
infeasibility flags, objective-component arithmetic and agreement with every
shard summary. There are 6,626 physical details: all 6,580 unique labels
including base, plus 46 byte-identical base duplicates. Every detail reports
zero infeasibility. Every source contingency also has a successful native
record; maximum native residual is 9.993582496148345e-6. This is complete
post-run evidence, but it was audited after the timed run had stopped.

| Scenario 095 result | Objective | Timing acceptance |
|---|---:|---|
| V16 | 356,907.86098102527 | Accepted, 297.7028850999923 s |
| V19 | No complete official score | Failed worker-shutdown gate, 297.4473472000027 s |
| V20 | 375,973.1919114017, reconstructed after the run | Failed shard-finalization gate, 298.01620340000227 s |
| Published fifth-best target | 434,319.522756719 | Comparison target only |

V20's retained feasible candidate improves the raw objective by
19,065.330930376425 over V16, but still trails the target by
58,346.33084531728 and is not a valid on-time top-five result. Mean contingency
bus penalties are 54,584.60349800306: active 15,423.75717106015 and reactive
39,160.84632694291. Runtime/finalization and solution quality both remain open;
solving the former alone would not meet the target. The other four scenarios
were not rerun in this resumption. All five top-five gates remain open.

Compact status archive:
`docs/evidence/C2FEN19402_TOP5_V20_EVALUATION_TIMEOUT_20260916.json`, SHA256
`aa3c5156ca73accfd26203c05c730a770c4c05aa76740c9c0bedb6e22ddb8afa`.
Post-run audit:
`docs/evidence/C2FEN19402_TOP5_V20_POSTRUN_AUDIT_20260916.json`.
All V20 solution vectors remain, as do V16's accepted on-time solution vectors.
The original failed status is unchanged. No further full run has been launched.

After verifying the committed V19 archive/status hashes and preserving the
complete V20 candidate, V19's superseded solution-text payloads were pruned:
13,206 entries, about 10.945 GiB net recovered. Its logs, original failed
status and partial evaluator records remain. See the cleanup report for
the exact target, checks and retention limitations. This supersedes the
earlier V19 retention statement above; V16, V18 and V20 vectors are untouched.

## V21 registration: earlier evaluator preparation within measured time

V20 completed all native work and evaluator shards but ran out of its unchanged
298-second evaluation allowance during shard finalization. The nine extra
evaluator processes were imported at the tail (about 1.1-1.3 seconds of ready
wait each), followed by first-shard evaluations taking about 4.2-7.4 seconds.
Starting those processes earlier is a targeted scheduling change, not a claim
that it alone suffices for the five-minute or top-five gates.

V21 adds an explicit opt-in `prepare_evaluator_pool_early` flag. After the cold
base result, still INSIDE the end-to-end timed run, the controller creates
the eventual 12-process evaluator pool and waits for readiness asynchronously.
The original three-process evaluation cap, subsequent idle-worker promotion
policy and 12-process tail cap are unchanged. Unpromoted workers wait idle;
no model is solved or evaluated as a warmup. Each run creates a fresh pool,
with no old solution, cached dispatch or external run state. Nothing is moved
outside the measurement boundary. The native solver/binary, evaluator
equations, exact case, constraints, tolerances, full label set, 300-second
total limit, evaluation/finalization reserves and 30 GiB free-space floor are
unchanged. Ordinary configurations do not enable this opt-in.

The finalization timer is now retained per shard. An explicitly diagnostic
queue/finalization snapshot is saved on success/failure; pending tasks never
count as certified by that snapshot. A diagnostic-write failure cannot skip
process shutdown. Four additional tiny tests cover early preparation with
unchanged actual concurrency, expired-start refusal, mandatory persistent
protocol and CLI cold/deadline invariants. All 100 Python tests passed.

Registered config: `config/c2fen19402_top5_early_evaluator_v21.json`.
One full cold scenario-095 experiment is planned in the previously absent
`runs/top5_cold_20260915/v21_s095`, only after the final component gates and a
pushed freeze. V16 remains the accepted on-time study incumbent; V20 remains
a higher-scoring complete retained candidate audited after the deadline.
Neither is used to initialize V21. All five top-five targets remain open.

## V21 fully verified result: better on-time incumbent

The single cold run of `0c30ab46038c128c50dd67c90cdd05bdac8905e6`
passed every existing gate in **297.47237920000043 seconds**. All 6,579 source
contingencies completed; all 6,580 unique official detail labels (including
base) match the raw CON set exactly. Official infeasibility is zero. Maximum
independent contingency residual is 9.993580819933623e-6, below the unchanged
1e-5 acceptance limit. Source/model/config/binary hashes match the frozen
manifest. All 6,580 root solution vectors are retained.

| Scenario 095 | Official objective | End-to-end seconds | Acceptance | Top five |
|---|---:|---:|---|---|
| V16 | 356907.86098102527 | 297.7028850999923 | PASS | No |
| V20 | 375973.1919114017 (post-run reconstruction only) | 298.01620340000227 | Failed finalization deadline | No |
| V21 | 375925.97863455745 | 297.47237920000043 | PASS | No |
| Published fifth-best target | 434319.522756719 | -- | Comparator | -- |

V21 improves the accepted on-time V16 score by 19018.117653532187, but remains
58393.54412216152 below the target. V20's slightly higher retrospective score
does not supersede the on-time acceptance requirement. These are individual
cold algorithm experiments, not repeated timing samples or causal speedup
estimates. No other scenario has been run with V21.

The early evaluator pool had 12 ready processes, while actual evaluation
concurrency retained the original caps. All 47 shard finalizations completed
with 2.833523 seconds remaining to the 298-second evaluation cutoff; final
shutdown, certification and serialization finished inside 300 seconds.

The audited base objective is 214720.19410102538 and mean contingency objective
161205.7845335321. Mean active/reactive imbalance penalties are respectively
15454.896052768887 and 39176.94355693413. GO2 feasibility permits these bounded,
penalized imbalances; passing does not mean zero imbalance or global optimality.

The compact archive is `docs/evidence/C2FEN19402_TOP5_V21_VERIFIED_20260916.json`
(SHA256 `4b3a45d05280c3429cc56f67db30c0b5d53857e15cdb4a68a637b4867ab3fa47`).
Status SHA256 is `c61aeb2885b587a62c36adc70dfe5f164b6f0660ee23ad6d331b6f59ceb8a076`.
The independently reconstructed component audit is
`docs/evidence/C2FEN19402_TOP5_V21_AUDIT_20260916.json`.

Read-only native-log attribution found 707 tasks with nonzero Jacobian
preparation, totaling 546.652037263 worker-seconds. These include local
refactorizations, not just the 24 resident initial factorizations. Economic
polish totaled 1219.433608429 worker-seconds (nested timers, not wall time).
Tasks consuming all three economic iterations contribute 41556.923874389575
of the 54631.83960970302 mean bus penalty. Inspection shows the economic path
currently ignores a contingency-specific factorization already built during
feasibility repair. Reusing that matching factorization is the next proposed
quality correction; it does not require another factorization or prior-run
initialization, and still needs tiny tests and a new frozen cold experiment.

## V22 registration: reuse the already-built feasibility Jacobian

In cached-economic mode, economic cleanup now preferentially uses the valid
contingency-specific factorization already constructed during that same
outage's feasibility repair. The cache records which branch was absent when
it was factorized. It is eligible only for the matching outage, with coupled,
active and reactive factors all valid. A post-outage factor must not have the
branch subtracted a second time. If no eligible factor exists, the existing
base-factor path remains unchanged. No new factorization, extra polish
iteration, prior-run solution, or external initialization is introduced.

The existing exact nonlinear acceptance checks and incumbent retention still
apply to every trial, as do source PMIN, ramp and voltage bounds, controls,
contingency set and economic objective. This is a different search direction,
not an algebraically equivalent speedup: higher score must be measured, not
assumed. The new boolean `economic_polish_reused_feasibility_jacobian` is
preserved in full, compact and worker logs to establish actual use.

The tiny regression forces a selected local refactorization, checks unchanged
feasibility-stage iterations/refresh counts, confirms one fewer branch-removal
update, independently validates the returned state and objective against a
full AC rebuild, retains the original base, and prohibits economic work on a
direct-only verification. Reusing the same resident solver on another outage
must match an independent solver for that outage; a previous outage's local
factorization must not leak into it. Existing copy/injection/early-rejection
oracle fixtures retain their old factor-selection option to keep those
equivalence tests isolated.

After all component tests pass and this revision is pushed, perform exactly
one cold scenario-095 experiment using
`config/c2fen19402_top5_reuse_feasibility_jacobian_v22.json`, in
`runs/top5_cold_20260915/v22_s095`. Configuration differs from V21 only in its
description; the algorithm change is pinned by the new revision/executable.
V21's early evaluator pool, 300-second end-to-end budget, 30-GiB storage floor,
10+40-second primary refinements and full official verification are unchanged.
V21 remains the retained accepted solution until a completed replacement is
proven better. All five top-five goals remain open.

V22 pre-run gates: all four native CTest groups and all 100 Python component
tests passed; `git diff --check` is clean. Native executable SHA256 is
`3521afedead17b1b297629403f64eda5ee4d9138343e7b03d18d6956ebb76171`;
configuration SHA256 is
`1191f8d9daab71d79dd3c37dfcf5d6985ceaac31364fe0b91761a9b523293986`.
No full V22 scenario had been run when these gates were recorded.
